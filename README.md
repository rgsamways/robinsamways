# robinsamways

Personal site + portfolio. Changes are proposed via OpenSpec (`openspec/changes/`) before implementation — see `CLAUDE.md` for session conventions. Contemporaneous R&D notes ("Sreditor") live in `docs/sreditor/`.