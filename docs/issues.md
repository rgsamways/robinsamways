# Issues / QA notes

Quick "take a look at this and tell me what's missing or wrong" capture — screenshots, console warnings, small visual bugs. This is not the Sreditor R&D log (`docs/sreditor/`) and not a formal OpenSpec change — just a running list Robin flags as he spots things in the running app, for CLI to pick up and check off.

Each entry includes the literal handoff text given to CLI, not just a summary, so this file is a self-contained record of what CLI was actually told.

## Open

- [ ] 2026-07-11 — New OpenSpec change `sreditor-page-content` proposed and validated (not a bug — logged here per the handoff-logging convention). Replaces the `/method/sreditor` placeholder with real content — the first Method-type page to actually use the required PROBLEM → EXISTING_APPROACHES → HYPOTHESIS → METHOD → RESULTS → CONCLUSION structure. Content is a verbatim, voice-adapted brief Robin brought back from building Sreditor for real tonight, in its own separate repository. Full detail in `openspec/changes/sreditor-page-content/`.

  **Handoff given to CLI (2026-07-11):**
  > Rewrite `web/src/app/method/sreditor/page.tsx` from its placeholder to the six required sections using the verbatim copy in `design.md` — copy is final, not a first draft, flag a genuine typo rather than silently rewording anything substantive (same convention as `farpost-page-content`). Match the existing `SectionHeader`/curly-quote conventions. Add a local `HamburgerMenu` linking to all six sections. Update Method's index entry for Sreditor (new teaser + tags: TypeScript, Node.js, Claude API, OpenSpec). Verify: all six sections render in order, local menu links work, index entry updated, `npm run build` clean, check for JSX whitespace-glue issues proactively. Before archiving, run `scc` and log to both `docs/metrics.md` and `docs/metrics.json`.

- [x] 2026-07-10 — New OpenSpec change `add-automated-test-suites` proposed and validated (not a bug — logged here per the handoff-logging convention). No test coverage existed anywhere in this codebase as a persistent, re-runnable suite — confirmed directly (no Playwright config or `.spec.ts` files anywhere in `web/`, no `tests/` directory or `pytest` dependency anywhere in `api/`; only `pieces/farpost-pulse-func/` had anything that survives, and even that wasn't on a real test framework). Established real Vitest + Playwright suites for `web/`, pytest for `api/`, and `node:test` for `pieces/farpost-pulse-func/`, plus the ongoing convention that new feature work includes tests as part of its own change. Full detail in `openspec/changes/archive/2026-07-11-add-automated-test-suites/`.

  **Handoff given to CLI (2026-07-10):**
  > Six task groups: set up Vitest for `web/` with representative unit tests (at minimum `loanApplication.ts`'s comparator); set up Playwright properly for `web/` (formalizing what's already been used ad hoc) with e2e specs covering global navigation plus one deep flow (Credential Flow's live demo, or Farpost Pulse's roster → detail → coaching-tip flow); set up pytest for `api/` with a dev-only dependency split (`requirements-dev.txt` or similar) so Railway's production build is unaffected, unit tests for `moderation.py`'s word-boundary matching (including the Scunthorpe-problem case), and at least one integration test via `TestClient`; convert `pieces/farpost-pulse-func/scripts/checkSeedShape.js` and `testHandlers.js` into real `node:test` files with a real `npm test` script; rewrite `docs/testing.md` to describe the new actual state and add the new convention to `CLAUDE.md`; verify everything runs clean. Before writing `web/` test code, check `web/AGENTS.md`'s reminder to read Next.js 16's own current testing docs rather than assuming prior-version conventions. Before archiving, run `scc --dryness web/src api pieces` and log to `docs/metrics.md` — expect a real code-volume increase this time (test files are genuine new code), call that out explicitly in the delta note. `npm run build` clean, check off tasks as you go.

  **Resolution:** all six task groups implemented and independently re-verified before archiving — 49 tests total (Vitest 11, pytest 9, `node:test` 29), `pytest` correctly isolated to `requirements-dev.txt` (confirmed absent from production `requirements.txt`), the two old ad-hoc scripts genuinely converted rather than duplicated, `docs/testing.md` and `CLAUDE.md` both rewritten to reflect the new reality, `scc` snapshot independently re-run and matched the logged numbers exactly (DRYness held flat at 62% despite the expected code-volume increase). Archived as `2026-07-11-add-automated-test-suites`.

- [ ] 2026-07-10 — `/ops/deploy` drifted from `docs/deployment-guide.md` again — Part 8a gained a missing step (seeding the real Cosmos DB locally before deploy, since there's no seed-triggering endpoint on the deployed Function App and the seed script is deliberately excluded from what ships to Azure) and its remaining steps renumbered from 1-5 to 1-7.

  **Handoff given to CLI (2026-07-10):**
  > Sync `web/src/app/ops/deploy/page.tsx`'s Part 8a (Farpost Pulse/Azure) against `docs/deployment-guide.md`'s current version: new step 2 ("Locally, in `pieces/farpost-pulse-func/`: `cp local.settings.json.example local.settings.json`, fill in `COSMOS_CONNECTION_STRING`... then `npm run seed`"), inserted between "get the connection string" and "deploy the source" — the rest of the steps shift down accordingly (deploy is now 3, app settings 4, CORS 5, Vercel env var 6, verify 7). Update the "Last updated" date if it changes. `npm run build` clean, no console warnings.

## Resolved

- [x] 2026-07-11 — New OpenSpec change `dev-log-content` proposed and validated (not a bug — logged here per the handoff-logging convention). Replaces the Dev Log placeholder with real content: a growing layman's-terms glossary, a public-facing Testing & Verification writeup (now honestly accurate since `add-automated-test-suites` landed), a real code-metrics dashboard sourced from a new `docs/metrics.json`, and two bug-log entries adapted from real Sreditor entries. Full detail in `openspec/changes/dev-log-content/`.

  **Handoff given to CLI (2026-07-11):**
  > Six task groups: build the Glossary section with 5 starter terms (Twilio, OAuth 2.0 Client Credentials Flow, SOQL, Field History Tracking, NFC/RFID); adapt `docs/testing.md` into a public-facing Testing & Verification section; create `docs/metrics.json`, backfill all five existing snapshots (cross-check against `docs/metrics.md`'s tables directly), and build a Metrics dashboard component reading it at build time; adapt both existing Sreditor entries (Cosmos throughput, Flex Consumption zero-functions) into public-facing bug-log entries; add a local `HamburgerMenu` linking to all four sections; verify. Write representative test coverage for anything with real logic (e.g. metrics data loading), per the new "tests ship with the feature" convention. Before archiving, run `scc` and log to both `docs/metrics.md` **and** the new `docs/metrics.json`. `npm run build` clean, check off tasks as you go.

  **Resolution:** all six task groups implemented. Glossary and Bug Log ship as growable data arrays (`web/src/components/dev-log/glossary.ts`, `bugLog.ts`) rather than hardcoded JSX, so future entries don't require restructuring. `docs/metrics.json` backfilled with all 5 prior `scc` snapshots, each cross-checked directly against its `docs/metrics.md` table before transcription; `parseMetricsSnapshots` (the one piece of genuinely new logic — file I/O separated from parsing so it's unit-testable) got 7 Vitest tests covering the happy path, missing/malformed fields, and multi-entry ordering. `MetricsDashboard`/`MetricsTrendChart` read `docs/metrics.json` via `fs.readFileSync` at Next.js build time (Server Component, no runtime API), rendering stat tiles, three small single-accent trend charts (Code lines, Complexity, DRYness — kept as small multiples rather than one dual-axis chart), and a reverse-chronological note log. Verified via headless browser: all four sections render, the local `HamburgerMenu` links to each, the dashboard's 6 stat/chart/log values were visually cross-checked against `docs/metrics.md` pixel-for-pixel. `npm run build` clean, full `web/` test suite (18 unit + 7 e2e) passes. `scc` snapshot logged to both `docs/metrics.md` and `docs/metrics.json` (76 files, DRYness flat at 62%). **Flagged, not yet verified:** `docs/metrics.json` sits outside `web/`'s Vercel Root Directory (`web/`) — the build-time file read works in local `npm run build` but may need Vercel's "Include source files outside of the Root Directory" project setting enabled to work on an actual Vercel deploy; worth a preview-deploy check before merging to production. Committed locally (`fd64a5e`), not yet pushed or archived.

- [x] 2026-07-10 — New OpenSpec change `project-page-parent-links` proposed and validated (not a bug — logged here per the handoff-logging convention). Individual project pages' local menus (Credential Flow, Farpost Pulse) only linked to their own on-page sections — no way back to the Narrative index except the browser back button or the global menu. Full detail in `openspec/changes/archive/2026-07-10-project-page-parent-links/`.

  **Handoff given to CLI (2026-07-10):**
  > Add a link back to `/narrative` in both Credential Flow's and Farpost Pulse's local `HamburgerMenu` link lists, alongside their existing section links — labeled clearly as "Narrative," distinct from the section links. Farpost's local menu is untouched (no parent index to link back to). `npm run build` clean, check off tasks as you go.

  **Resolution:** CLI added `{ href: "/narrative", label: "Narrative" }` as the first entry in both pages' `SECTION_LINKS` arrays, ahead of the existing section links. Verified via headless-browser check that both menus show the section links plus "Narrative," and confirmed Farpost's own local menu was untouched. Drift-audited directly (grepped both files, confirmed exact placement and correct `href`), `npm run build` clean, `scc` snapshot logged (flat at 62% DRYness, as expected for a two-line change), archived and pushed.

- [x] 2026-07-10 — The landing page's local section menu (Origin Story, Architecture, Tech Stack, Design Notes, Tech Roster) links only to scroll-anchors on that same page — it never links to the Dashboard route (`/narrative/farpost-pulse/dashboard`), a third real page a visitor has no way to discover without the direct URL. The Tech Roster cards get a visitor to individual tech pages; nothing gets them to the dashboard.

  **Handoff given to CLI (2026-07-10):**
  > Add a way to reach `/narrative/farpost-pulse/dashboard` from the landing page — either as a sixth entry in the existing local `HamburgerMenu` (distinct from the five scroll-anchor entries, since it's a real navigation link, not a same-page anchor) or as its own visible link/button near the Tech Roster section (e.g. "View team dashboard →"). Use judgment on which reads better; either is fine as long as the dashboard is actually discoverable without knowing the URL. `npm run build` clean, no console warnings.

  **Resolution:** Went with a visible button near Tech Roster rather than a sixth `HamburgerMenu` entry, since that menu's five existing links are same-page scroll-anchors and a real route change felt like a different kind of action worth its own affordance. Added a `Link` to `/narrative/farpost-pulse/dashboard` right after the `TechRoster` grid in `web/src/app/narrative/farpost-pulse/page.tsx`, styled with the same outlined-button pattern already used for `TechDetail`'s coaching-tip button and the homepage contact form (`border border-accent … hover:bg-accent hover:text-background`), labeled "View team dashboard →" per the suggested wording. Verified via a local dev server (port 3001 — 3000 was held by Farpost's own dev server): the link renders on the landing page and `/narrative/farpost-pulse/dashboard` returns 200. `npm run build` clean, no console warnings.

- [x] 2026-07-10 — The real Function App deploy (`func azure functionapp publish farpost-pulse-func`) reported success and Azure's own Deployment Center showed "Succeeded (Active)," but the live Log stream showed the host finding zero functions: `Reading functions metadata (Custom)` → `0 functions found (Custom)` → `0 functions loaded`, and every endpoint (confirmed via `GET /api/techs`) 404d. Caught live while walking through the actual production deploy for the first time.

  **Handoff given to CLI (2026-07-10):**
  > `pieces/farpost-pulse-func/.funcignore` excludes `node_modules`, but this deploy used `func azure functionapp publish` with no `--build remote` flag, so it went through local build, not Azure's own Oryx/remote build — the deploy log itself confirms this: `remotebuild = false`, `Skipping oryx build (remotebuild = false)`. With `node_modules` excluded from the local zip *and* no remote build to reinstall it server-side, the deployed package almost certainly has the four function files but no `@azure/functions`/`@azure/cosmos` packages to `require()` — every function file fails to load, which matches "0 functions found" exactly.
  >
  > Fix: remove `node_modules` from `.funcignore`, so the already-installed local dependencies ship as part of the local-build zip deploy. Confirm nothing else in `.funcignore` needs the same reconsideration.
  >
  > Verify: after the fix, Robin will redeploy and confirm the Log stream shows `4 functions found` / `4 functions loaded`, and `GET .../api/techs` returns real seeded data instead of 404.

  **Resolution:** CLI removed `node_modules` from `.funcignore` (confirmed `@azure/cosmos` and `@azure/functions` were both present in local `node_modules` and would now ship with the local-build zip), reviewed the rest of the file, and confirmed nothing else needed the same fix (`*.js.map`, `.git*`, `.vscode`, `local.settings.json(.example)`, `test`/`tests`, `scripts`, `.gitignore` are all dev-only or secret files with no runtime role, unlike `node_modules`, which the deployed code actually `require()`s). Robin redeployed — package size jumped from 11.2 KB to 8.2 MB, consistent with dependencies now being included. Verified end-to-end from this session: Log stream showed all four functions registered with real invoke URLs (`techs`, `techJobs`, `coachingGenerate`, `dashboardPatterns`); `GET /api/techs` returned 200 with six real seeded technicians; the tech detail page rendered real job history, a trend chart showing genuine variation, and a real computed coaching tip distinct per tech (confirmed on two techs — one showing "steady, no red flags," the other correctly identifying its seeded recurring weakness, "most commonly missed shot recently is interior kitchen"). Farpost Pulse is fully live in production.

- [x] 2026-07-10 — `npm run seed` fails against the real, free-tier Cosmos DB account (`farpost-pulse-cosmos`, 1000 RU/s total throughput cap) with `BadRequest`/substatus 1028: "would have increased the total throughput to 1200 RU/s." Caught while actually running the seed step during deployment, not in dev — this never surfaced against the in-memory fake client `scripts/testHandlers.js` uses.

  **Handoff given to CLI (2026-07-10):**
  > `pieces/farpost-pulse-func/scripts/seed.js`'s `main()` creates the database with `client.databases.createIfNotExists({ id: DATABASE_NAME })` — no throughput specified — then `ensureContainer()` creates all three containers (`techs`, `jobs`, `coachingHistory`) each with their own implicit dedicated throughput (Cosmos defaults new containers to 400 RU/s when neither the database nor the container specifies shared throughput). 3 × 400 = 1200 RU/s, over the free tier's 1000 RU/s account-wide cap.
  >
  > Fix: give the database shared throughput at creation (`client.databases.createIfNotExists({ id: DATABASE_NAME, throughput: 1000 })`), and leave `ensureContainer()`'s per-container calls exactly as they are — with the database on shared throughput, containers created without their own `throughput` option draw from the shared pool instead of getting dedicated allocation. Confirmed the deployed Function App's own `src/lib/cosmosClient.js` never calls `createIfNotExists` (it only references containers by name, assuming they already exist), so this bug is isolated to the seed script — no other file needs touching.
  >
  > Verify: `npm run seed` succeeds against the real `farpost-pulse-cosmos` account without a throughput error, and confirm in the Azure Portal (Cosmos DB account → Data Explorer, or → Settings → Scale) that the database shows 1000 RU/s shared throughput with all three containers drawing from it, not each holding its own allocation.

  **Resolution:** applied the one-line fix in `pieces/farpost-pulse-func/scripts/seed.js` exactly as specified. Before running the real verification, checked the live account read-only first and found the earlier failed run had already left real state behind: the `farpost-pulse` database existed with no database-level throughput, and `jobs`/`techs` already had their own dedicated 400 RU/s offers locked in from creation (`coachingHistory` never got created — that's where the original run hit the 1200 RU/s cap). Since `createIfNotExists` is a no-op on existing resources, the code fix alone couldn't retroactively change already-provisioned throughput — flagged this to Robin and got explicit confirmation before deleting the existing database (seed data only, fully regenerable) and letting `npm run seed` recreate it fresh. Verified against the live account: `npm run seed` completed with no throughput error (6 techs, 144 jobs upserted); a follow-up read-only check confirmed the database now carries a 1000 RU/s shared offer and none of the three containers have their own dedicated offer (`readOffer()` returns `resource: undefined` for each, meaning they draw from the shared pool). Used small temporary Node scripts against the real `@azure/cosmos` SDK for both the inspection and the deletion step, deleted immediately after use — not part of the app.

- [x] 2026-07-10 — `/ops/deploy` drifted from `docs/deployment-guide.md` again, on top of the 2026-07-07 sync below. This one's from adding a new Part 8 (Portfolio piece deployments, with a Farpost Pulse/Azure subsection) ahead of the actual build, per Robin's request to nail this convention down now rather than let it accumulate. The page also had two other stale spots unrelated to Part 8, caught while making this edit.

  **Handoff given to CLI (2026-07-10):**
  > Sync `web/src/app/ops/deploy/page.tsx` against `docs/deployment-guide.md` (source of truth):
  > 1. **New Part 8 — Portfolio piece deployments**, with its Farpost Pulse (Azure) subsection — add this section to the page in full, matching the guide's wording.
  > 2. **Troubleshooting renumbered** from Part 8 to Part 9 (it now comes after the new Part 8) — update the page's heading to match.
  > 3. **Intro line** now says "the five core services ... plus each promoted portfolio piece's own infrastructure as one gets added (see Part 8)" instead of "across five services" — update to match.
  > 4. **Part 7 verification checklist**: the menu-navigation line now says "Home / Method / Narrative / Farpost / Dev Log" (was still "Portfolio / Farpost / Dev Log placeholder routes," stale since the Method/Narrative restructure); a new checklist line was added about a deployed piece loading real data.
  > 5. **"Last updated" date** on the page should become 2026-07-10.
  >
  > This is documentation ahead of the actual Farpost Pulse deployment — the guide's new Part 8 describes what *will* need doing once `pieces/farpost-pulse-func/` exists and is deployed, not something to execute yet. Just sync the page's content to match the guide. `npm run build` clean, check for the JSX whitespace-glue bug proactively, no console warnings.

  **Resolution:** synced all five items into `web/src/app/ops/deploy/page.tsx`: added the new `PART_8_PORTFOLIO_PIECE_DEPLOYMENTS` section in full (the "already provisioned" resource list plus the 5-step deploy procedure for Farpost Pulse/Azure), renamed the old `PART_8_TROUBLESHOOTING` section id/heading to `PART_9_TROUBLESHOOTING`, updated the intro paragraph and the "Last updated" date to 2026-07-10, and updated Part 7's checklist (menu-nav line now reads "Home / Method / Narrative / Farpost / Dev Log," plus the new "deployed piece loads real data" line). Also caught and fixed a genuine numbering slip in the *source* guide itself while syncing — `docs/deployment-guide.md`'s new Part 8 contained a subsection labeled "9a" instead of "8a" (inconsistent with the `6a`/`6b` convention already used for Part 6's subsections) — fixed the guide first, then mirrored the corrected "8a" into the page. Caught a real instance of the recurring JSX whitespace-glue bug during verification: `<strong>Trigger a new deploy after adding it</strong> — env` rendered as `...adding it— env` with the space before the em-dash silently dropped — fixed with an explicit `{" "}`. Verified via a full-text dump of the rendered page (confirmed no other glued-dash spots) and zero console warnings. `npm run build` clean.

- [x] 2026-07-07 — `/ops/deploy` still drifted from `docs/deployment-guide.md` in three places, on top of the Part 6a/8 sync just completed. Two were flagged by CLI as out-of-scope-but-noticed; one (Part 3) hadn't been surfaced yet because the guide edit happened after the original sync handoff was written.

  **Handoff given to CLI (2026-07-07):**
  > Three more content-only syncs needed between `docs/deployment-guide.md` (source of truth) and `web/src/app/ops/deploy/page.tsx`, same pattern as the Part 6a/8 sync you just finished:
  > 1. **Part 3 (Vercel setup)** — the guide's step 4 now says `NEXT_PUBLIC_API_URL=https://api.robinsamways.ca` is a required Production environment variable (with the localhost-fallback/CORS-failure explanation and the "env var changes don't apply to existing deployments" note), replacing the old "no environment variables are required yet... for later" wording. The page still has the stale version.
  > 2. **Part 6b intro (Resend/outbound)** — guide now says the contact form is Resend's first real runtime caller, replacing "not needed by anything currently built."
  > 3. **Part 7 (verification checklist)** — the email-test checklist item now specifies testing from an account other than `rgsamways@gmail.com`, per the self-send-loop caveat.
  >
  > Update the "Last updated" date on the page if it changes. Same verification bar as last time: `npm run build` clean, check for the JSX whitespace-glue bug proactively, no console warnings.

  **Resolution:** synced all three into `web/src/app/ops/deploy/page.tsx`, plus one more stale spot caught in the same Part 6b block while editing it: the guide's step 6 now reads "Store the same key on `/web` only if a build-time need arises — as of the `contact-form` change, only `/api` sends email" but the page still said "No send-email code exists in `/api` yet" (stale — `contact-form` had already shipped) — fixed to match. Part 3's env var step, Part 6b's intro paragraph, and Part 7's email-test line all now match the guide's current wording exactly. `npm run build` clean, zero console warnings, verified via a full-text dump alongside the 2026-07-10 sync above (same file, same pass).

- [x] 2026-07-10 — New OpenSpec change `farpost-pulse-build` proposed and validated (not a bug — logged here per the handoff-logging convention). Replaces the Farpost Pulse placeholder with the real app: three routes (landing page with case-study narrative + tech roster, a per-tech detail page with job history/coaching-tip generation/trend chart, and a dashboard page), backed by a genuine Node.js Azure Functions app calling the already-provisioned Cosmos DB and (eventually) Azure OpenAI resources — kept as a real, separate deployable on Azure rather than folded into this repo's own `api/`, per `CLAUDE.md`'s "Portfolio piece isolation" convention and because the whole point of this demo is proving hands-on Node/Azure serverless experience. AI coaching-tip generation is mocked for this change (Azure OpenAI's model deployment is blocked on a quota increase, 1-2 business day turnaround as of 2026-07-10); the real call is isolated in one function for an easy later swap. Also corrects the `/narrative` index's Farpost Pulse teaser, which incorrectly said "the real build ... is a separate, already-scoped project" — a stale framing from earlier confusion this session, now fixed as part of this change rather than patched separately. Full detail in `openspec/changes/farpost-pulse-build/`.

  **Handoff given to CLI (2026-07-10):**
  > Four task groups: build the Azure Functions backend at `pieces/farpost-pulse-func/` in this repo (a piece-specific folder under `pieces/`, sibling to `web/` and `api/` at the repo root — git-tracked here, only its deploy target is Azure, not its home repo) targeting the already-provisioned `farpost-pulse-func` Azure resource — Cosmos DB wiring against `farpost-pulse-cosmos`, seed data with intentional patterns, the four HTTP endpoints, a mocked `generateCoachingTip()`, rate limiting on the one write endpoint, anonymous auth, documented CORS requirements — this is source code delivered by the change, not a live Azure deployment, which is Robin's manual step afterward; build the three Next.js routes in this repo (`web/src/app/narrative/farpost-pulse/page.tsx` rewritten from its placeholder, plus new `[techId]` and `dashboard` routes), calling the Function App directly from the browser via a `NEXT_PUBLIC_FARPOST_PULSE_API_URL` env var, no proxy through this repo's own `/api`; update the `/narrative` index's Farpost Pulse teaser and tags (Azure Functions, Cosmos DB, React — Azure OpenAI excluded until the real call is wired in); verify. Full detail and exact scenarios in `openspec/changes/farpost-pulse-build/tasks.md` and `specs/`.
  >
  > Before archiving this change, run `scc --dryness web/src api pieces` and log the snapshot to `docs/metrics.md` per `CLAUDE.md` — this is the first snapshot to include `pieces/`, so `farpost-pulse-func/`'s Node.js code shows up as a new language row for the first time. `npm run build` clean in `/web`, no console warnings, check off tasks as you go.

  **Resolution:** built the full app across all four task groups. `pieces/farpost-pulse-func/`: a real Node.js Azure Functions v4 app (`src/functions/{techs,techJobs,coachingGenerate,dashboardPatterns}.js`), a Cosmos DB client (`src/lib/cosmosClient.js`, three containers per the spec'd partition keys), a per-IP rate limiter mirroring Credential Flow's exact 5-req/60s threshold, and `generateCoachingTip()` isolated behind a `// TODO: replace with real Azure OpenAI call` comment. Seed data (6 techs, 24 jobs each, `scripts/generateSeedData.js`) is a pure, seeded-random function validated with zero live Cosmos connection via `scripts/checkSeedShape.js` before any SDK code was written, per the migration plan's own suggested order — confirmed the strong tech hits ≥90% completion, the weak-angle tech's most-missed angle is genuinely `roofline`, the slow-turnaround tech averages ≥28h, and all three "improving" techs show a genuine detected improving trend. Went a step further for full endpoint verification: wrote `scripts/testHandlers.js`, which intercepts `@azure/functions`' registration and monkeypatches the Cosmos client with an in-memory fake container (same "swap the shared client, test the real logic" pattern already used for Credential Flow's Salesforce integration) — exercises all four handlers directly, including the rate limiter tripping at the 6th request and a coaching tip that genuinely references computed stats. For frontend integration testing, went further still: wrapped the *real* handler functions in an actual local HTTP server (translating Node's `http` req/res to the Azure Functions v4 handler signature) so the three new Next.js routes could be verified against genuine business logic over real HTTP, not a second hand-written mock — caught two real issues this way: (1) the spec's "tag completion" language turned out to mean NFC-tag completion specifically, distinct from "angle type" (the dashboard spec lists them as two separate items) — fixed `computeAngleCompletionRate` vs. a new `computeTagCompletionRate`, using the latter for the roster snapshot stat and dashboard bar chart; (2) the tech-detail trend chart's `aria-label` only reported first/last values, which can coincidentally match even when the chart shows real variation (confirmed on an "improving" tech whose tag-completion series dipped to 50% mid-run but started/ended at 100%) — fixed by having `TrendChart` derive its label from the whole series' min/max/direction, and separately switched that specific chart to turnaround hours (a smoother, more clearly "trending" seeded metric than the noisy discrete tag counts), which the spec explicitly allows ("tag completion *or* turnaround"). Built the landing page's origin story as a curiosity-driven learning narrative — no external company or individual referenced, framed around closing a genuine hands-on-experience gap (React, Node, Azure serverless, an AI SDK) through real building rather than a tutorial. Verified end-to-end via headless browser against the real-handler-backed local server: all three routes render correctly, the coaching tip button shows a loading state then a real generated tip (keyboard-activatable via Enter, confirmed), the trend chart and dashboard bar chart both carry accessible `aria-label`s, and the `/narrative` index shows the corrected teaser and all three tags with zero trace of the old "separate project" framing. `npm run build` clean, zero console warnings. Ran `scc --dryness --exclude-dir .git,.hg,.svn,node_modules,.venv web/src api pieces` (the plain command from the handoff actually pulled in `pieces/farpost-pulse-func/node_modules` — ~4,500 files of vendored `@azure/*` package content — since scc's default `--exclude-dir` list is only `.git,.hg,.svn` and its `.gitignore`-based exclusion didn't reliably reach node_modules from this scan root; caught it by checking the file count before trusting the number, fixed with an explicit exclusion, and updated the documented command in `docs/metrics.md` so future runs don't hit the same thing) and logged the corrected snapshot. Not verifiable without Robin's real Azure/Cosmos access: the actual live Function App deployment, real CORS configuration on Azure, and production data — all his manual steps per design.md's explicit scope.

- [x] 2026-07-10 — New OpenSpec change `method-narrative-navigation` proposed and validated (not a bug — logged here per the handoff-logging convention). Splits the generic Portfolio index into two typed indexes, Method and Narrative; relocates Sreditor and Credential Flow (renamed from Salesforce Loan Demo) into them; adds the Farpost Pulse placeholder; retires `/portfolio` outright. Full detail in `openspec/changes/method-narrative-navigation/`.

  **Handoff given to CLI (2026-07-10):**
  > Eight task groups: relocate Sreditor from `/sreditor` to `/method/sreditor` (content unchanged); relocate Salesforce Loan Demo from `/portfolio/salesforce-loan-demo` to `/narrative/credential-flow`, renaming its heading and metadata title from "$ Portfolio" to "$ Credential Flow" in the same pass; build `/method` (index, one entry: Sreditor) and `/narrative` (index, two entries: Credential Flow with tags — Salesforce, OAuth 2.0, Anthropic AI — and a Farpost Pulse placeholder), reusing the existing data-driven index + local `HamburgerMenu` pattern; add the `/narrative/farpost-pulse` placeholder route (coming-soon only, real build is separate/future); delete `web/src/app/portfolio/` entirely, no redirect; update `MenuToggle.tsx`'s global links to Home/Method/Narrative/Farpost/Dev Log. Before archiving, run `scc --dryness web/src api` and log the snapshot to `docs/metrics.md` per `CLAUDE.md`. `npm run build` clean, check off tasks as you go.

  **Resolution:** moved `web/src/app/sreditor/page.tsx` to `web/src/app/method/sreditor/page.tsx` verbatim, and `web/src/app/portfolio/salesforce-loan-demo/page.tsx` to `web/src/app/narrative/credential-flow/page.tsx` — same move, except its `<h1>` and `metadata.title` now read "Credential Flow" instead of the stale "Portfolio" (this also closes the heading-mismatch item flagged from the prior navigation change). Everything else on that page — all 7 sections, the live demo widget, relationship view, setup gallery, and its own local section menu — moved untouched. Built `web/src/app/method/page.tsx` and `web/src/app/narrative/page.tsx` as small data-driven indexes (a `PROJECTS` array of `{slug, title, teaser, tags?}`), each with its own local `HamburgerMenu`; Narrative's entries use the exact given teaser text for Credential Flow (leading with "Salesforce Loan Demo —" as a demoted subtitle, per the proposal's own framing) plus its three tags, and a placeholder-appropriate teaser for Farpost Pulse (no tags). Added the `/narrative/farpost-pulse` placeholder matching the existing "$ Title" / "// coming soon" pattern. Deleted `web/src/app/portfolio/` entirely. Updated `MenuToggle.tsx`'s global link list to Home/Method/Narrative/Farpost/Dev Log. **The Method and Narrative intro sentences and Sreditor's teaser are my first-pass drafts**, not final, per the task's own flag — happy to take exact wording instead. Verified via headless browser: `/portfolio` and `/portfolio/salesforce-loan-demo` both 404; the global menu lists all five items in the new order and navigates correctly; Method's index shows its one entry and local menu works; Narrative's index shows both entries with tags visible only on Credential Flow; Credential Flow's page shows its new `<h1>`/tab title ("Credential Flow" in both), all content including live demo data against a mock, and all 7 section-menu links jumping correctly; the Farpost Pulse placeholder renders; Farpost itself is untouched (still 4 sections). Hit one stale-cache build error along the way — a leftover `.next` type-validator file still referencing the deleted `/portfolio` route from a prior dev run — cleared `.next` and rebuilt clean, not a real code issue. Ran `scc --dryness web/src api` and logged the new snapshot to `docs/metrics.md`: +2 files, +82 code lines, +3 complexity, DRYness essentially flat (65% → 64%, within noise) — consistent with a routing/index restructure (content moved, not duplicated) rather than new logic. `npm run build` clean, zero console warnings.

- [x] 2026-07-10 — New OpenSpec change `method-narrative-navigation` proposed and validated (not a bug — logged here per the handoff-logging convention). Splits the just-archived generic Portfolio index into two typed indexes, Method and Narrative, organized by what kind of story a project page tells rather than chronology: Method for pages documenting a genuine technical experiment (Sreditor, relocated from its old top-level placeholder), Narrative for pages telling the story of something built for a specific real reason (Credential Flow — renamed from Salesforce Loan Demo — and a new Farpost Pulse placeholder). Also formally specs, for the first time, the PROBLEM → EXISTING_APPROACHES → HYPOTHESIS → METHOD → RESULTS → CONCLUSION section structure for future Method-type content, adds an optional `tags` field to index entries in place of a third nav tier, and fixes a pre-existing stale `/portfolio` route reference in the `salesforce-loan-demo` spec left over from the prior change. Full detail in `openspec/changes/method-narrative-navigation/`.

  **Handoff given to CLI (2026-07-10):**
  > Eight task groups: relocate Sreditor from `/sreditor` to `/method/sreditor` (content unchanged); relocate Salesforce Loan Demo from `/portfolio/salesforce-loan-demo` to `/narrative/credential-flow`, renaming its heading and metadata title from "$ Portfolio" to "$ Credential Flow" in the same pass (this also closes out the stale-heading item flagged in this file from the prior change — no separate fix needed); build `/method` (index, one entry: Sreditor) and `/narrative` (index, two entries: Credential Flow with tags — Salesforce, OAuth 2.0, Anthropic AI — and a Farpost Pulse placeholder), both reusing the existing data-driven index + local `HamburgerMenu` pattern from the prior change; add the `/narrative/farpost-pulse` placeholder route (coming-soon only — the real Farpost Pulse build is a separate future change, out of scope here); delete `web/src/app/portfolio/` entirely, no redirect; update `MenuToggle.tsx`'s global links to Home/Method/Narrative/Farpost/Dev Log. Full detail and exact scenarios in `openspec/changes/method-narrative-navigation/tasks.md` and `specs/`.
  >
  > Before archiving this change, run `scc --dryness web/src api` from the repo root and log the snapshot to `docs/metrics.md`, per the convention now in `CLAUDE.md` — same checkpoint as the drift audit. `npm run build` clean, no console warnings, check off tasks as you go.

- [x] 2026-07-10 — New OpenSpec change `project-navigation-restructure` proposed and validated (not a bug — logged here per the handoff-logging convention). Extracts a reusable `HamburgerMenu`, relocates the Salesforce Loan Demo case study to its own route, rebuilds `/portfolio` as a showcase index, adds local per-page navigation to Portfolio/Salesforce Loan Demo/Farpost, and adds Sreditor's global nav entry + placeholder route. Full detail in `openspec/changes/project-navigation-restructure/`.

  **Handoff given to CLI (2026-07-10):**
  > Six task groups: extract a reusable `HamburgerMenu` component from `MenuToggle`, relocate the Salesforce Loan Demo content to `/portfolio/salesforce-loan-demo` (a move, not a rewrite), rebuild `/portfolio` as a showcase index, add local heading-menus to Portfolio/Salesforce Loan Demo/Farpost, add Sreditor's global nav entry + placeholder route, then verify everything. One flagged item: task 3.1's Portfolio index copy is a first pass, not final — say so if you'd rather I hand you exact wording instead of drafting it yourself. `npm run build` clean, check off tasks as you go.

  **Resolution:** extracted `web/src/components/HamburgerMenu.tsx` (generic `links`/`ariaLabel` props) from `MenuToggle.tsx`'s existing button+dropdown; `MenuToggle` is now a thin wrapper passing its hardcoded global link list (Home/Portfolio/Farpost/Dev Log/Sreditor) with `ariaLabel="menu"` — chosen specifically so the composed `"Open menu"`/`"Close menu"` strings stay byte-identical to the pre-refactor button, preserving existing behavior exactly (no click-outside/Escape handling added, per the explicit non-goal). Added an optional `id` prop to `SectionHeader` (plus `scroll-mt-4` so a jumped-to heading doesn't land flush against the viewport edge) and threaded `id`s through every section on the Salesforce Loan Demo and Farpost pages (including the two sections that live inside `PortfolioDemos.tsx` and the one inside `SetupGallery.tsx`). Moved `/portfolio/page.tsx`'s entire prior content verbatim to `web/src/app/portfolio/salesforce-loan-demo/page.tsx`, adding only the `id`s and a local `HamburgerMenu` (7 section links) — no other change, including the page's own `<h1>` text ("$ Portfolio") and metadata title, left exactly as they were per the explicit "verbatim/renders identically" instruction. **Flagging this one**: the relocated page's heading and browser-tab title still literally say "Portfolio," not "Salesforce Loan Demo" — technically correct per the instruction, but worth a look since it reads oddly on a URL that isn't `/portfolio` and now duplicates the index page's title; happy to fix in a follow-up once confirmed it's actually wanted, rather than deciding unilaterally that the "no content changes" rule didn't cover the heading. Rebuilt `/portfolio` as a small data-driven index (a `PROJECTS` array of `{slug, title, teaser}`, currently one entry) with its own local `HamburgerMenu` (linking to each project page) — adding a second project later is just another array entry, no restructuring. **The index's intro sentence and teaser copy are my first-pass draft**, not final, per the task's own flag — happy to take exact wording instead. Added Sreditor to the global menu after Dev Log and created `web/src/app/sreditor/page.tsx` as a placeholder, matching the exact "$ Sreditor" / "// coming soon" pattern Portfolio and Farpost both used. Verified via headless browser: the global menu lists all five items in order, opens/closes (aria-label toggling correctly), closes on link click, and navigates correctly (confirmed with `waitForURL` after an initial ambiguous-locator false alarm in my own test script, not a real app issue); the Portfolio index's local menu and teaser link both work; `/portfolio/salesforce-loan-demo` renders identically to the old `/portfolio` (Overview through Setup Gallery, live demo widget hitting a mock and showing real data) with all 7 section links present and jumping to the correct target; Farpost's local menu's 4 section links jump correctly; the Sreditor placeholder renders. Zero console warnings throughout. `npm run build` clean, all 9 routes present.

- [x] 2026-07-08 — Two small date corrections, bundled into one handoff: the `farpost-page-content` `ORIGIN_STORY` copy had the wrong year (2025 instead of 2026) in two places, and the homepage Experience section's Farpost date range was wrong for the same reason.

  **Handoff given to CLI (2026-07-08):**
  > 1. **Fix the already-shipped Farpost page copy** — Robin confirmed the dispatch app was built in late May 2026, not 2025, and the TapLog-to-Farpost synthesis happened mid-June 2026. `design.md`'s Final Copy section is already fixed (both dates now say 2026). Update `web/src/app/farpost/page.tsx` to match — same two date mentions, everything else in that paragraph unchanged.
  > 2. **Resume date fix** — new OpenSpec change `fix-farpost-start-date` corrects the homepage's Farpost entry from "2025 — Present" to "2026 — Present" in `web/src/components/resume/Experience.tsx`. Don't touch Impres Pharma's "2012 — 2025" or any other entry — only the Farpost date was wrong.
  >
  > Verify: `/farpost`'s origin story reads 2026 in both places; the homepage Experience section shows "2026 — Present" for Farpost with every other entry unchanged. `npm run build` clean.

  **Resolution:** both were one-line date swaps. `web/src/app/farpost/page.tsx`'s two mentions ("In late May 2025" → "In late May 2026", "arrived at by mid-June 2025" → "arrived at by mid-June 2026") now match `design.md`'s corrected Final Copy exactly; nothing else in either sentence touched. `web/src/components/resume/Experience.tsx`'s Farpost `dates` field changed from `"2025 — Present"` to `"2026 — Present"`; every other role's date range (Impres Pharma's `"2012 — 2025"` included) left untouched. Verified via headless browser on both pages: `/farpost`'s two date sentences read correctly, the homepage's Farpost row shows "2026 — Present" and the Impres Pharma row still shows "2012 — 2025", zero console warnings. `npm run build` clean.

- [x] 2026-07-08 — New OpenSpec change `farpost-page-content` proposed and validated (not a bug — logged here per the handoff-logging convention). Replaces the `/farpost` page's placeholder with four fully-drafted sections: `ORIGIN_STORY`, `PROBLEMS_FARPOST_SOLVES`, `BUILDING_LIFECYCLE_EXAMPLE`, `PROCESS`. Full detail in `openspec/changes/farpost-page-content/`.

  **Handoff given to CLI (2026-07-08):**
  > Work through `tasks.md` — section 3 (lightbulb status update) is already done, so just sections 1 and 2. All copy is final and verbatim in `design.md` — this is placement/styling, not wordsmithing; flag anything that looks like a typo rather than silently rewording. Key style note: match the existing `SectionHeader` convention from `/portfolio`, and style the `BUILDING_LIFECYCLE_EXAMPLE` timeline consistently with `RelationshipView.tsx`'s existing status-history display rather than a new component. `npm run build` clean, no console warnings, check off tasks as you go.

  **Blocker encountered and resolved:** on first read, `design.md` didn't actually contain the drafted copy it was supposed to (only Context/Goals/Decisions/Risks) — searched the whole repo for any trace of it (including the originating lightbulb, which explicitly left the actual drafting as a separate task) and found nothing, so stopped and flagged it back rather than inventing first-person copy on Robin's behalf. Robin/Chat added a `## Final Copy` section to `design.md` with the actual verbatim text; re-read it and continued.

  **Resolution:** rewrote `web/src/app/farpost/page.tsx` end to end — four `SectionHeader`-marked sections in the specified order, `PROBLEMS_FARPOST_SOLVES`'s five bolded lead-ins as `<strong>` sub-headings within one section (not five top-level sections), and `BUILDING_LIFECYCLE_EXAMPLE`'s dated entries rendered with the exact same `›`-prefixed `<ul>`/`<li>` shape already used for `RelationshipView.tsx`'s status-history timeline (accent-colored date, em-dash, then the entry text) rather than a new component. The fictional-example disclaimer sits in its own bordered callout (`border-l-4 border-accent bg-skills-bg`, the same treatment already used by `components/ops/Callout.tsx`) directly above the timeline, so it can't be missed. Converted straight quotes/apostrophes in the source copy to the site's existing curly-quote HTML-entity convention (`&rsquo;`/`&ldquo;`/`&rdquo;`, confirmed by grepping how `portfolio/page.tsx` already does it) — a presentation-convention match, not a wording change; every actual word is untouched. Caught and fixed a real instance of the recurring JSX whitespace-glue bug during verification: `{"R&D"} work` rendered as `R&Dwork` with the space silently dropped (the adjacent `{"SR&ED"} tax` on the very next line rendered fine with the same-looking pattern, consistent with this bug's documented unpredictability) — fixed with an explicit `{" "}`. Proofread the whole rendered page against `design.md`'s `## Final Copy` programmatically: extracted and normalized both texts and diffed word-by-word — the only differences were the intentional quote-style conversion and the diff script's own list-marker handling, zero actual content/wording differences, so nothing to flag as a typo. `npm run build` clean, zero console warnings, full-page screenshot confirms the callout, sub-headings, and timeline styling all read correctly., never-customized `create-next-app` icon (a white triangle) — Robin wants it replaced with a mark matching the site's own branding: an orange `$` on a black circle.

  **Handoff given to CLI (2026-07-08):**
  > Replace `web/src/app/favicon.ico` with a proper site icon: a black circle background with the `$` character centered on it, in the site's accent color (`--accent: #d97706` in `globals.css`) — echoing the "`$` Robin Samways" header treatment already used elsewhere. No image-editing tool needed: use Next.js's file-based icon convention — add `web/src/app/icon.tsx` using `ImageResponse` from `next/og` to code-generate the icon (a styled `<div>` with the black circle background and the orange `$`, no external image asset). Check current Next.js docs on whether a static `favicon.ico` should stay alongside `icon.tsx` for broader browser-tab compatibility, or whether `icon.tsx` alone is sufficient — follow whatever the framework's own convention recommends rather than guessing.
  >
  > Verify: the new icon renders correctly in a browser tab (favicon) across at least two browsers; no console warnings/errors from the new icon route; `npm run build` clean.

  **Resolution:** checked the framework's own docs (`node_modules/next/dist/docs/.../app-icons.md`) before writing anything, per the note in the handoff — `favicon.ico` and `icon.tsx` are alternative file conventions, not complementary ones ("You cannot generate a `favicon` icon. Use `icon` or a `favicon.ico` file instead."); `icon.tsx` alone produces a proper `<link rel="icon">` tag, so no reason to keep both. Deleted `web/src/app/favicon.ico` and added `web/src/app/icon.tsx`: a 32×32 `ImageResponse` — a `black` circle (`borderRadius: "50%"`) with the `$` centered in `#d97706` at `fontSize: 20`, `fontWeight: 700`. `npm run build` picked it up automatically as a new `/icon` static route. Verified the actual generated PNG directly (fetched `/icon`, confirmed 32×32 `image/png`, then rendered it upscaled 10× via a headless browser to inspect it closely — clean circle, correctly centered/colored `$`, no clipping or antialiasing artifacts). Verified across two real browser engines via Playwright (downloaded a matching Firefox binary, since the one cached on this machine was a version behind what this Playwright install expected): both Chromium and Firefox report the identical `<link rel="icon" href="/icon?..." type="image/png" sizes="32x32">` tag and both successfully fetch the icon directly (200, `image/png`, 799 bytes, byte-identical). `npm run build` clean, no console warnings.

- [x] 2026-07-08 — Add a curated screenshot gallery to `/portfolio`, sourced from 6 PNGs Robin captured tonight (`docs/screens/`), showing the real Salesforce Setup work and live data behind this case study.

  **Handoff given to CLI (2026-07-08):**
  > Add a new section to `web/src/app/portfolio/page.tsx`, placed **after `RELATIONSHIP_VIEW`, at the very end of the page** (not before `LIVE_DEMO` — the two live interactive demos should be the reader's first experience; this gallery is corroborating "here's the real Setup work" detail for someone who wants to dig deeper afterward, not a prelude). Header text: `SETUP_GALLERY`, matching the existing `##`-marker section convention.
  >
  > Six source images are at `docs/screens/` — move them into `web/public/images/salesforce-setup/` (new subdirectory) and reference via `next/image`. Filenames and captions, in display order:
  >
  > 1. **`flow.png`** — "The Record-Triggered Flow that stamps a Decision Date the instant a Loan Application's Status changes to Approved or Denied. It runs as a 'Before Save' flow — since it only needs to set a field on the record already being saved, there's no reason to pay the overhead of a separate 'after save' update. Two entry conditions (Status = Approved OR Status = Denied) keep it from evaluating on every save, and it's optimized for Fast Field Updates specifically because 'set one field' is exactly the operation that optimization exists for."
  > 2. **`fields.png`** — "The full field list on the custom `Loan_Application__c` object: a lookup to the standard Contact object (Applicant), a lookup to the standard Account object, a Currency field for Amount Requested, a Status picklist, and two Date fields for Submitted Date and Decision Date. Deliberately unglamorous — the point of this case study isn't an exotic data model, it's demonstrating that the same Account/Contact/custom-object relationship pattern used in any real Salesforce implementation works end-to-end here, from Setup through to a live API."
  > 3. **`oauth.png`** — "The External Client App's OAuth configuration — Client Credentials Flow enabled, a dedicated 'Run As' integration user, and IP Relaxation allowing the API server's requests through. This is the server-to-server authentication described above in practice: no user ever logs in through a browser, and the FastAPI backend authenticates directly against this configuration on every token request."
  > 4. **`record-details.png`** — "A real Loan Application record — Jack Willis's $3,150 request against the Willis Estate account, currently Under Review. Every value shown, including the resolved Applicant and Account lookups, was created through this site's own live demo widget against this Salesforce org, not staged or mocked."
  > 5. **`record-related.png`** — "Salesforce's own Field History Tracking, not a derived or fabricated timeline: this record's Status moved from Submitted to Under Review, and Salesforce logged the transition automatically — old value, new value, the user who made the change, and a timestamp. This is the real audit trail behind the status history shown in the Relationship View above, built on the same 'computed from actual events, not a stored score' principle described in the Farpost parallel."
  > 6. **`railway.png`** — "Production environment variables for the `/api` Railway service, values masked — `SALESFORCE_DOMAIN`, `SALESFORCE_CLIENT_ID`, `SALESFORCE_CLIENT_SECRET`, and `ANTHROPIC_API_KEY` among them. This is the only place real credentials exist outside Salesforce's and Anthropic's own consoles: never committed to the repo, never exposed to the browser, injected at deploy time."
  >
  > **UI pattern — thumbnails that open into a dialog, not full-size embeds:** these screenshots are ~1900-2500px wide; embedded at full size they'd be unreadable and would blow out the page layout, especially on mobile. Render a responsive thumbnail grid (e.g. 3 columns desktop, 2 tablet, 1 mobile — use your judgment on exact breakpoints matching the site's existing responsive conventions), each thumbnail showing the image plus a short label. Clicking/tapping a thumbnail opens a modal dialog: the full image (scaled to fit the viewport, not the image's native size), the caption text below it, a dimmed backdrop behind it. **Must work well on mobile, not just desktop** — the dialog should size itself to the viewport (not assume desktop width), and dismissal must not depend on a keyboard: support a visible close button/×, tap-on-backdrop-to-dismiss, *and* Escape for keyboard/desktop users. Match the site's existing monospace/single-accent-color styling.
  >
  > Verify: all 6 thumbnails render and open their correct image+caption; dialog dismisses via close button, backdrop click, and Escape; test at a mobile viewport width specifically (not just resized desktop) to confirm the dialog and thumbnail grid both remain usable — image scales to fit width, text stays legible, close button is reachable. `npm run build` clean.

  **Resolution:** moved the 6 PNGs from `docs/screens/` into `web/public/images/salesforce-setup/` and built `web/src/components/portfolio/SetupGallery.tsx`, wired into `portfolio/page.tsx` as a `<SetupGallery />` section right after `<PortfolioDemos />` (which holds `LIVE_DEMO`/`RELATIONSHIP_VIEW`) — the last section on the page, as specified. Thumbnail grid is `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`, each `next/image` thumbnail using that image's actual width/height (read via .NET's `System.Drawing` since neither PIL nor a JS image library was on hand) so the aspect ratio is correct rather than assumed. Clicking a thumbnail opens a `role="dialog"`/`aria-modal` overlay: a plain `<img>` (not `next/image`, since the modal needs simple `object-contain`/`max-h-[70vh]` viewport-fit scaling rather than a fixed intrinsic size) plus the caption below it, dismissible via a visible `×` close button, a click on the dimmed backdrop (stopped from bubbling when the click originates inside the panel itself), or Escape. Verified all 6 thumbnails open the correct image `src` and caption text (spot-checked two source screenshots directly against their captions beforehand — `flow.png` genuinely shows a Record-Triggered Flow with 2 conditions optimized for Fast Field Updates, `record-related.png` genuinely shows a Submitted→Under Review Field History entry — before trusting the rest). All three dismissal paths confirmed independently (Escape after each of the 6, close button, backdrop click). Also verified at a real mobile viewport (390×844, `isMobile`/`hasTouch` Playwright context, not just a resized desktop window) — thumbnail width fits inside the viewport, the dialog sizes itself to the viewport rather than assuming desktop width, the close button remains tappable, and tap-on-backdrop dismisses via `touchscreen.tap`. `npm run build` clean, zero console warnings on both desktop and mobile passes, no JSX whitespace-glue issues in the caption text (checked directly, since these are long strings with embedded punctuation).

- [x] 2026-07-08 — None of the create-form's abuse-resistance measures (honeypot, rate limiting, and the incoming profanity blocklist) are ever surfaced to a visitor — they're all invisible, server-side-only protections. Robin wants this called out near the form; it's a good signal of engineering maturity for an interviewer skimming the live widget.

  **Handoff given to CLI (2026-07-08):**
  > Add a small, muted-text note near `LoanDemoWidget.tsx`'s create-row (below the table is probably the natural spot, but use judgment on exact placement) stating the abuse-resistance measures already in place: something like *"Protected against spam and abuse: a honeypot field, per-visitor rate limiting, and a profanity blocklist all run server-side before anything reaches Salesforce."* Bundle this with the profanity-blocklist handoff below if you haven't started that one yet, so the copy and the feature ship together — don't claim the blocklist exists in copy before the backend actually does. Match existing muted/small-text styling used elsewhere on the page (e.g. the loading/error states).
  >
  > Verify: the note is visible near the create-row, doesn't crowd the table/pagination controls, and — if built alongside the blocklist handoff — the claim is actually true (a blocked-word submission really does get rejected). `npm run build` clean.

  **Resolution:** added the note verbatim as a `text-xs text-muted` paragraph directly below the pagination controls in `LoanDemoWidget.tsx` — shipped in the same turn as the profanity-blocklist backend (see the entry below), so the claim was true the moment it went live, never a copy-before-feature gap. Verified the note renders with the exact specified text and sits below the table/pagination without crowding it. `npm run build` clean.

- [x] 2026-07-08 — `POST /salesforce/loan-applications` has no content moderation on `applicant_name`/`account_name` — a visitor could type vulgar/offensive text into either free-text field, which then displays publicly on `/portfolio` in both `LoanDemoWidget` and `RelationshipView`, and gets sent as context to the Anthropic recommendation endpoint. Robin wants a blocklist, same approach as a past project ("Mr.Commish").

  **Handoff given to CLI (2026-07-08):**
  > Add a server-side profanity blocklist check to `create_loan_application`/`POST /salesforce/loan-applications` in `api/app/salesforce.py`, applied to `applicant_name` and `account_name` only (not `amount_requested`, which is numeric). Reject with the existing generic 422 ("Invalid submission") if either field matches — don't reveal that it's specifically a profanity check, same principle as not leaking why other validation failed.
  >
  > 1. Add a small local module (e.g. `api/app/moderation.py`) holding a `BLOCKED_WORDS` set — no external moderation API call, no new dependency, self-contained like the rest of this codebase. Source the word list from an established public-domain list (e.g. the commonly-used "LDNOOBW" — List of Dirty, Naughty, Obscene, and Otherwise Bad Words — MIT/CC0-licensed English list on GitHub) rather than hand-authoring one; trim/curate as you see fit.
  > 2. **Match on word boundaries, case-insensitive** (`\bword\b`-style, not a plain substring check) — a naive substring match produces false positives on innocuous names that happen to contain a blocked word as part of a longer word (the classic "Scunthorpe problem"). Whole-word matching only.
  > 3. Apply the check only at creation — `PATCH` never touches these fields, so nothing else needs it.
  >
  > Verify: a submission with a blocked word in either field is rejected with the same generic 422 as other invalid submissions; a legitimate name that merely *contains* a blocked substring inside a longer, innocuous word is NOT rejected (test at least one such case); normal valid submissions are unaffected. `npm run build`/tests clean.

  **Resolution:** added `api/app/moderation.py`, sourced verbatim from the real LDNOOBW English word list (fetched from GitHub, 403 entries) rather than hand-curated, minus one overly generic two-letter entry (`"xx"`) trimmed for false-positive risk. Compiled into a single `\b(?:word1|word2|...)\b` regex, case-insensitive, sorted longest-first; `contains_blocked_word(text)` returns whether any entry matches on a whole-word/whole-phrase boundary. Wired into `post_loan_application` in `api/app/salesforce.py` — `contains_blocked_word(applicant_name)` / `contains_blocked_word(account_name)` added to the existing validation `or` chain that already raises the generic 422 ("Invalid submission"), so a blocked-word rejection is indistinguishable from any other invalid-submission rejection, per the "don't reveal it's specifically a profanity check" instruction. `PATCH` untouched, since it never accepts these fields. Verified via `TestClient`: a blocked word in `applicant_name` alone → 422; a blocked word in `account_name` alone → 422; "Scunthorpe Smith" / "Essex Rural Holdings" (both genuinely contain blocked substrings — "cunt", "sex" — as part of longer, innocuous words) → 201, actually created, confirming the word-boundary match doesn't false-positive on the classic Scunthorpe problem; a normal valid submission → 201, unaffected. `npm run build` (`/web`, unaffected) clean; API import and TestClient suite clean.

- [x] 2026-07-08 — Neither `LoanDemoWidget`'s flat table nor `RelationshipView`'s per-Account table has pagination — both render every row from the already-fetched, sorted/filtered list at once. Robin wants paging at 10 records per page plus a total-results counter on each.

  **Handoff given to CLI (2026-07-08):**
  > Add client-side pagination (10 records per page) to both `LoanDemoWidget.tsx`'s flat table and `RelationshipView.tsx`'s per-Account table. Both already share `loanApplication.ts` for types/constants/the sort comparator — add a shared `PAGE_SIZE = 10` constant there too rather than duplicating the number.
  >
  > 1. Add `page` state (starting at 1) to each component. Slice the existing sorted+filtered array to the current page's 10 records for rendering — don't change how sorting/filtering themselves work, just what subset of the result renders.
  > 2. Below each table, add a "Showing X–Y of Z" counter plus Previous/Next controls (disable Previous on page 1, disable Next on the last page), matching the site's existing monospace/single-accent-color styling — no need for numbered page buttons, Prev/Next is enough at this scale.
  > 3. **Reset to page 1 whenever the underlying result set changes**: `LoanDemoWidget` when `statusFilter` changes; `RelationshipView` when `selectedAccountId` or its own status filter changes. Otherwise a visitor can land on a page number that's now out of range for the new, smaller filtered set.
  > 4. **Clamp the current page after a mutation** (delete especially) — if the current page no longer exists after `applications`/`groups` shrinks (e.g. you were on page 2 of 2 and deleted the only record on that page), fall back to the new last valid page rather than showing an empty page with a working Previous button that goes nowhere useful.
  > 5. `LoanDemoWidget`'s inline create-row (and its success/error message row) and `RelationshipView`'s Account/filter selectors are **not** part of the paginated data — they always render above the current page's rows, unaffected by paging.
  >
  > Verify: with more than 10 applications in a filtered view, only 10 show per page and the counter/Prev/Next reflect the true total; changing the status filter resets to page 1; deleting the last record on the last page clamps back to a valid page instead of showing empty; the create-row stays visible regardless of which page you're on. `npm run build` clean.

  **Resolution:** added `PAGE_SIZE = 10` to `loanApplication.ts`. Both components gained `page` state, a `totalItems`/`totalPages` computation over the already-sorted-and-filtered array, and a `pagedApplications` slice (`(page - 1) * PAGE_SIZE` to `page * PAGE_SIZE`) used only for the rendered rows — sort/filter logic itself untouched. Two small effects per component: one resets `page` to 1 when the relevant filter state changes (`statusFilter` in `LoanDemoWidget`; `selectedAccountId` or `statusFilter` in `RelationshipView`), and one clamps `page` down whenever `totalPages` shrinks (`setPage((current) => Math.min(current, totalPages))`) — this single effect generically covers "clamp after a mutation" without a delete-specific special case, since any mutation that shrinks the visible/filtered set changes `totalPages` and triggers the same clamp. Added a "Showing X–Y of Z" counter plus Previous/Next buttons (`type="button"`, so they don't trigger `LoanDemoWidget`'s form submission) below each table; the create-row and the Account/filter selectors sit outside the paginated slice entirely, so they're unaffected by paging by construction. Verified against a local mock with 23 records in one group: page 1 showed 10 rows with "Showing 1–10 of 23"; Next correctly advanced to "Showing 11–20 of 23" with the right applicants visible; switching the status filter reset to page 1 with an accurate counter for the narrower set ("Showing 1–4 of 4"); navigating to the last page (3 of 3, "Showing 21–23 of 23") and deleting all 3 rows there clamped back to "Showing 11–20 of 20" / "page 2 of 2" rather than showing an empty page; `RelationshipView`'s pagination worked identically on its own grouped data. `npm run build` clean, zero console warnings.

- [x] 2026-07-08 — Chrome DevTools Issues tab (not Console — no errors/warnings there) flags "A form field element should have an id or name attribute," 7 violating nodes, on `/portfolio`. Cause: the per-row Status-update `<select>` in `LoanDemoWidget.tsx` (the one that changes an existing record's status) has no `id`/`name` at all — likely unnoticed because the whole table got wrapped in one `<form>` for the inline create-row work, sweeping every field inside it into the browser's form-autofill scan. Both of `RelationshipView.tsx`'s selects (`account-select`, `relationship-status-filter`) already have proper ids and aren't affected.

  **Handoff given to CLI (2026-07-08):**
  > `LoanDemoWidget.tsx`'s per-row Status-update `<select>` (around line 363, inside the `sortedApplications.map` — the one wired to `handleStatusChange`, distinct from the create-row's `#loan-status` select which already has an id) has no `id` or `name` attribute, which Chrome's Issues tab flags across every non-Archived row. A static id would create duplicate-id violations across rows, so give it a **unique per-row** value instead — e.g. `` name={`status-${app.id}`} `` (or `id`, either satisfies the check). Purely a browser-advisory fix, no functional behavior change.
  >
  > Verify: Chrome DevTools Issues tab no longer reports this issue on `/portfolio`; the per-row Status select still works identically (same `onChange`/PATCH behavior). `npm run build` clean.

  **Resolution:** added `name={`status-${app.id}`}` to the per-row Status `<select>` in `LoanDemoWidget.tsx` — unique per row (avoids the duplicate-id problem a static `id` would've caused), no other change. Couldn't drive Chrome's actual Issues-tab panel directly through Playwright: this specific advisory is generated by DevTools' autofill-heuristic scan, and listening for it via CDP's `Audits.issueAdded` (the protocol event the Issues tab is built on) produced zero events even against a deliberately bad control page (`<form><select>` with no id/name at all) — so the headless CDP path doesn't reliably surface this particular check regardless of whether the underlying page is fixed. Verified the actual condition Chrome's message describes instead: enumerated every `<input>`/`<select>`/`<textarea>` inside a `<form>` on `/portfolio` and confirmed each now has an `id` or `name` (7 fields total, 0 missing both — previously the two per-row Status selects, one per loaded application, had neither). Confirmed the per-row select still works identically: selecting a new status still calls `handleStatusChange`, the underlying mock's `PATCH` succeeds, the select's value updates to reflect the new status, and no error state appears. `npm run build` clean.

- [x] 2026-07-08 — `LoanDemoWidget` (flat table) and `RelationshipView` (Account-grouped view) each fetch their own independent copy of the Loan Application data — a mutation in one (create/status-update/delete) never reaches the other, so e.g. deleting a record leaves it visible in the Account dropdown's grouped data until an unrelated remount.

  **Handoff given to CLI (2026-07-08):**
  > `LoanDemoWidget.tsx` and `RelationshipView.tsx` (both under `web/src/components/portfolio/`) are sibling components on `web/src/app/portfolio/page.tsx`, each independently fetching the same underlying Loan Application data (`GET /salesforce/loan-applications` and `GET /salesforce/accounts` respectively). `RelationshipView`'s `useEffect` only fetches once on mount (`[]` dependency), so it never learns about a create/status-update/delete that happens in `LoanDemoWidget`. Robin specifically noticed this on delete, but the same staleness applies to create and status-update too — scoping the fix to all three mutations, not just delete.
  >
  > 1. Add an optional `onMutate?: () => void` prop to `LoanDemoWidget`, called right alongside its existing `loadApplications()` call after every successful create, status-update, and delete (three call sites: `handleSubmit`, `handleStatusChange`, `handleDelete`).
  > 2. Add an optional `refreshKey?: number` prop to `RelationshipView`, and include it in the dependency array of its data-fetching `useEffect` (alongside the existing mount-only fetch) so a change re-triggers `GET /salesforce/accounts`.
  > 3. In `web/src/app/portfolio/page.tsx`, add a `refreshKey` state (a plain incrementing number), pass `onMutate={() => setRefreshKey((k) => k + 1)}` to `<LoanDemoWidget />` and `refreshKey={refreshKey}` to `<RelationshipView />`.
  > 4. `RelationshipView`'s existing auto-select effect (`if (!selectedAccountId && groups.length > 0) setSelectedAccountId(groups[0].account_id ?? "")`) only handles the case where nothing is selected yet — after a refetch, also handle the case where the *currently selected* Account no longer exists in the new `groups` (e.g. its last Loan Application just got deleted): fall back to the first remaining group, or clear the selection if `groups` is now empty.
  >
  > Verify: creating, updating the status of, or deleting a record in the flat table causes the Account dropdown's grouped data to refresh without a page reload; deleting the only Loan Application tied to an Account removes that Account from the dropdown and the view falls back sensibly (first remaining Account, or the existing empty state if none remain) rather than showing a stale/broken selection. `npm run build` clean.

  **Resolution:** `LoanDemoWidget` gained an optional `onMutate?: () => void` prop, called after the existing `loadApplications()` in `handleSubmit` and `handleStatusChange`, and after the local `setApplications` filter in `handleDelete` (which never called `loadApplications` to begin with — it updates local state directly, so `onMutate` is the only signal a sibling gets for that mutation). `RelationshipView` gained an optional `refreshKey?: number` prop added to its data-fetching `useEffect`'s dependency array (`[refreshKey]`), so a change re-triggers `GET /salesforce/accounts`. Since `page.tsx` exports `metadata` and must stay a Server Component, the `refreshKey` state couldn't live directly in it — added a new small Client Component, `PortfolioDemos.tsx`, holding the `refreshKey` state and rendering both the `LIVE_DEMO` and `RELATIONSHIP_VIEW` sections (moved verbatim, including their copy) with the wiring between them; `page.tsx` now just renders `<PortfolioDemos />` and keeps its `metadata` export intact. `RelationshipView`'s auto-select effect now checks whether the currently selected Account still exists in the new `groups` on every re-run (not just whether nothing is selected yet) — falls back to the first remaining group if the selection is now stale, or clears to `""` if `groups` is empty. Verified via a local mock (in-memory records, three Accounts) driven through the actual `LoanDemoWidget` UI: deleting the only application tied to an Account (Lee Farms) removes it from the Account dropdown without a reload, and — checked specifically since it's the trickier case — deleting it *while it was the currently selected Account* falls back cleanly to the first remaining group rather than leaving a stale selection; a status update (Jane Doe → Approved) and a fresh create, both done through the flat table, are immediately visible in `RelationshipView`'s grouped data once selected. `npm run build` clean.

- [x] 2026-07-08 — `RelationshipView`'s per-Account table has no sort or filter, unlike `LoanDemoWidget`'s flat table — inconsistent, and worth matching now that both views sit on the same page.

  **Handoff given to CLI (2026-07-08):**
  > Bring `RelationshipView.tsx`'s per-Account applications table up to parity with `LoanDemoWidget.tsx`'s existing sort/filter UX:
  > 1. **Sortable columns**: same click-header-to-sort pattern as `LoanDemoWidget` (▲/▼ indicator, toggles direction on repeat click) across Applicant/Amount/Status/Submitted/Decision — apply it to `selectedGroup.applications` before rendering rows. `account_name` isn't a column here (it's implied by the selected Account), so the sortable field set is those 5, not `LoanDemoWidget`'s 6.
  > 2. **Status filter**: add the same `STATUS_FILTER_OPTIONS` (All + the 5 settable statuses + Archived) filter control above the table, scoped to the currently selected Account's applications (filter narrows `selectedGroup.applications`, sort orders what's left — same composition order as the flat table).
  >
  > Reuse the flat table's sort/filter logic/types where they line up rather than reimplementing from scratch, but keep the two components independent (don't introduce shared state beyond the `refreshKey`/`onMutate` wiring from the prior handoff).
  >
  > Verify: clicking each of the 5 column headers sorts ascending then descending on repeat click; the Status filter composes correctly with sorting (e.g. filter to Approved, then sort by Amount); switching the selected Account resets/reapplies sort and filter sensibly against the new group's applications. `npm run build` clean.

  **Resolution:** extracted the pieces that genuinely lined up between the two components into a new shared module, `web/src/components/portfolio/loanApplication.ts`: the `LoanApplication` type (previously duplicated identically), `SortDirection`, `SETTABLE_STATUSES`/`STATUS_FILTER_OPTIONS`, and a generic `compareLoanApplications(a, b, field, direction)` comparator (the same null-handling/number-vs-string logic `LoanDemoWidget` already had, now used by both). `LoanDemoWidget` was refactored to import from this module instead of defining its own copies — no behavior change there. `RelationshipView` gained its own `SortField` (5 fields, no `account_name` — it's implied by the selected Account, per the handoff), a `COLUMNS` array, `sortField`/`sortDirection`/`statusFilter` state, and a `handleSort` matching `LoanDemoWidget`'s toggle-on-repeat-click pattern; the table headers became the same clickable ▲/▼ buttons, and rows now render from `sortedApplications` (filter narrows `selectedGroup.applications`, sort orders what's left — same composition order as the flat table) instead of the raw group array directly. Added a filter `<select>` next to the existing Account `<select>`. The two components stay independent otherwise — no shared React state beyond the existing `refreshKey`/`onMutate` wiring. Verified via the local mock: each of the 5 column headers sorts ascending then descending on repeat click (confirmed via the ▲/▼ indicator and actual row order, checked concretely on Amount); filtering to a single status and then sorting composes correctly, using the coincidence that filtering "Young Ventures" to Denied still correctly showed only its one Denied application after a sort key was already active; switching the selected Account re-applies whatever sort/filter is currently set against the new group's applications, not the old one. `npm run build` clean, zero console warnings, no JSX whitespace-glue regressions in a full-text dump of the page.

- [x] 2026-07-07 — New OpenSpec change `salesforce-relationship-view` proposed and validated (not a bug — logged here per the handoff-logging convention). Extends `salesforce-loan-demo` with three read-only pieces: an Account-grouped view, an AI-assisted "recommended next action" via the Anthropic API, and a real Field-History-Tracking-backed status timeline. Bundled with a second, independent piece of UI polish (folding `LoanDemoWidget`'s standalone create form into an inline table row). Full detail in `openspec/changes/salesforce-relationship-view/`.

  **Handoff given to CLI (2026-07-07), piece 1 (salesforce-relationship-view):**
  > New OpenSpec change is ready at `openspec/changes/salesforce-relationship-view/` — extends `salesforce-loan-demo` with three read-only pieces: an Account-grouped view, an AI-assisted "recommended next action" via the Anthropic API, and a real Field-History-Tracking-backed status timeline. Read `proposal.md`/`design.md`/`specs/`/`tasks.md` there, and implement `tasks.md` sections 2–4 (API endpoints, web UI, verification). Section 1 (enabling Field History Tracking in Salesforce Setup) is mine — already done. Section 5 (Railway/Vercel deploy) is also mine, once your build is ready. Key design decisions from `design.md`: Anthropic client via raw `httpx` (matching `salesforce.py`'s existing pattern, not the SDK), model `claude-haiku-4-5`, and all three new endpoints are GET-only — no new write surface. [A real `ANTHROPIC_API_KEY` was supplied for local testing, added to `api/.env`, not `.env.example`, same pattern as the existing `SALESFORCE_*` vars.]

  **Handoff given to CLI (2026-07-07), piece 2 (inline create-row, independent, bundled into the same handoff):**
  > `LoanDemoWidget.tsx`: replace the standalone "create a demo application" form with an inline new-application row at the top of the table's `<tbody>`, aligned to the real columns: Applicant/Account/Amount/Status cells get the existing inputs directly (same state, same `validate()`/`handleSubmit`, same `POST /salesforce/loan-applications` payload — layout change only); Submitted/Decision cells are static `—`, not editable (both system-set); Actions cell gets a small submit control in place of the full-width "Create" button; the honeypot field stays off-screen, just relocated into the row; validation errors become condensed inline per-field messages; success/error feedback becomes a full-width message row (`colSpan` across all 7 columns) below the new-row, reworded since creation now happens inside the same table; on success, reset inputs to empty/Draft. Verify: relationship-view endpoints return real data grouped by Account, the recommendation endpoint returns a real Claude-generated suggestion, the history endpoint returns real Field History Tracking entries; creating via the inline row behaves identically to before (validation, honeypot, rate-limit); Submitted/Decision cells in that row never show inputs; sort/filter never touch the new-row itself. `npm run build` clean in `/web`. Check off `docs/issues.md` and `tasks.md` when done.

  **Resolution:** `api/app/ai.py` — new Anthropic client via raw `httpx` (`POST https://api.anthropic.com/v1/messages`, model `claude-haiku-4-5`, no SDK), smoke-tested against the **real** Anthropic API before building anything on top of it (replied `pong` to a literal echo prompt — confirmed the model name/API version/request shape are all correct). `api/app/salesforce.py` gained `list_loan_applications_by_account` (new `ACCOUNT_GROUPED_FIELDS` including the raw `Account__c` id), `get_status_history` (queries the real `Loan_Application__History` object, filtered to `Field = 'Status__c'`), `get_status_and_submitted`, and three new `GET`-only endpoints: `/salesforce/accounts` (server-side grouping by Account, order preserved), `/salesforce/loan-applications/{id}/history` (empty list — not an error — when no changes exist), `/salesforce/loan-applications/{id}/recommendation` (rate-limited via the same shared `_is_rate_limited`/`_client_ip` pool as create/update/delete, since it's the one new endpoint with real per-call cost; 404 for an unknown id, 422 for a malformed one, 502 without leaking the API key or raw response body on failure). Refactored the `record → LoanApplicationOut` mapping (previously duplicated) into a shared `_to_loan_application_out` helper used by both the flat list and the grouped view. **Caught a real, pre-existing production bug while working on this**: `api/app/main.py`'s CORS `allow_methods` was still `["GET", "POST"]` — the `PATCH`/`DELETE` endpoints added in the prior polish-pass session never got their methods added to CORS, so a real browser calling them cross-origin would fail at preflight. This slipped through last time because verification used `curl` and `TestClient` (neither enforces CORS) plus a mock server that was more permissive than the real API. Fixed by adding `PATCH`/`DELETE` to `allow_methods`, and this time explicitly verified the preflight for both. On the web side: built `RelationshipView.tsx` (Account `<select>`, grouped table, per-application "recommend"/"history" toggle buttons that fetch on demand — not automatically for every row — and cache their result until closed) and rewired `LoanDemoWidget.tsx`'s create form into an inline `<tbody>` row wrapped in a `<form>` around the whole table (native HTML doesn't allow `<form>` as a direct table descendant of `<tbody>`, so the form wraps the table instead, letting Enter-to-submit still work); the create-row's Submitted/Decision cells are always static `—`; success/error feedback is a `colSpan={7}` row below it; the create-row stays structurally first regardless of sort/filter state since it's rendered outside the sorted/filtered data-row list, not as one of the rows being sorted. Added portfolio copy (`## RELATIONSHIP_VIEW`) explicitly tying the Field History Tracking timeline back to the `FARPOST_PARALLEL` "computed from actual events, not a stored score" framing, and explaining the Anthropic-not-Agentforce honesty angle. Fixed a `Fragment`-without-`key` bug caught during my own review before it ever hit a browser (used shorthand `<>` inside `.map()`, which can't take a `key` prop — switched to explicit `<Fragment key={app.id}>`). Verified via FastAPI's `TestClient` with `_request` monkeypatched: `/salesforce/accounts` groups correctly (2 groups, order preserved, correct nested shape); history returns real old→new entries with timestamps and a genuine empty list (not an error) for an untouched application; malformed ids 422 on both endpoints; **the recommendation endpoint was tested against the real Anthropic API** (not mocked) and returned a genuinely useful, context-appropriate suggestion for an "Under Review" application; rate limiting confirmed shared and triggers 429 on the 6th request; CORS preflight confirmed for both PATCH and DELETE post-fix. Verified the full web UI against a comprehensive local mock (all 6 endpoint shapes) — caught and fixed an off-by-one path-parsing bug in my own mock server along the way (not the real app) before trusting the result: full-text dump shows no JSX whitespace-glue regressions; the inline create-row's Submitted/Decision cells always show `—`; creating via the row works end-to-end and the row stays first through subsequent sort and filter changes; the Account selector switches correctly between groups; the recommend button shows a loading state then real suggestion text; the history button shows real transition entries or the correct empty state. `npm run build` clean, zero console warnings. Not verifiable without Robin's real org: the three new endpoints against genuine Salesforce data, and the real Field History Tracking entries — his to confirm per section 5.

- [x] 2026-07-07 — `LoanDemoWidget`/`api/app/salesforce.py` polish pass: expose Status on create, sortable + filterable table, scoped Status-only update, and a client-side-only remove — agreed with Robin but not yet handed off.

  **Handoff given to CLI (2026-07-07):**
  > Several small, agreed additions to the existing `salesforce-loan-demo` capability (`web/src/components/portfolio/LoanDemoWidget.tsx` + `api/app/salesforce.py`), bundled into one handoff:
  >
  > 1. **Status on create**: add a `Status` `<select>` to the create form (Draft/Submitted/Under Review/Approved/Denied, defaulting to Draft) and thread it through to the API's create payload instead of hardcoding `"Status__c": "Draft"`. This is the highest-value one — it lets a visitor create a record already at Approved/Denied and watch the existing Decision Date Flow fire immediately.
  > 2. **Submitted Date bug**: `create_loan_application` currently never sets `Submitted_Date__c` at all, so new demo records show a blank Submitted date in the list. Set it server-side to today's date on creation — don't expose it as a user input, a real application system stamps this automatically.
  > 3. **Sortable columns**: click a column header to sort the table ascending/descending by that field. Client-side only, over the already-fetched list — no new endpoint needed.
  > 4. **Status filter**: a simple filter (pills or a `<select>`) above the table — All / Draft / Submitted / Under Review / Approved / Denied. Client-side only.
  > 5. **Scoped Status-only update**: add `PATCH /salesforce/loan-applications/{id}` accepting only a new `Status` value (nothing else editable), same rate-limiting as the create endpoint (reuse `_is_rate_limited`/`_client_ip`). Add a Status dropdown/action per row in the table wired to this endpoint, with loading/success/error state, refreshing that row (or the whole list) on success.
  > 6. ~~Client-side-only remove~~ — **superseded, see the 2026-07-07 correction below. Do not implement as originally written; a real delete endpoint replaces this.**
  >
  > Verify: creating with Status=Approved/Denied shows Decision Date populated immediately after creation (Flow fires on create too, not just update, per the existing Flow's "created or updated" trigger); Submitted Date is no longer blank on new records; sort and filter work correctly together (e.g. filtering to Approved then sorting by Amount); the PATCH endpoint is rate-limited and rejects any field other than Status. `npm run build` clean. Check off here when done.

  **Correction (2026-07-07) — real delete, gated by an Archived status, replaces item #6:**
  > Robin reconsidered the earlier "client-side-only remove" call (item #6 above, and its now-superseded Archived addendum) — he wants a **real delete**, not a local-only hide. This still has to avoid the original risk (public, unauthenticated write access to live Salesforce data with no ownership boundary), so the fix is a status-based guard instead of an auth boundary:
  >
  > 1. **Salesforce**: Robin is adding `Archived` as a sixth value on the `Status__c` picklist (Object Manager → Loan Application → Fields & Relationships → Status → New Value → "Archived") and has already manually set the original 3 seed records (LA-0001/0002/0003) to Archived in Setup. No Flow change — the Decision Date Flow only triggers on Approved/Denied.
  > 2. **`Archived` is deliberately excluded from every *settable* Status control** — the create-form dropdown (#1) and the per-row Status-update dropdown (#5) should offer only Draft/Submitted/Under Review/Approved/Denied, never Archived. This means nothing in the UI can ever archive a record or un-archive one — Archived is reachable only by Robin editing Salesforce directly. Do include Archived as an option in the read-only Status *filter* (#4), so visitors can still filter the table to see the archived seed records.
  > 3. **Real delete endpoint**: add `DELETE /salesforce/loan-applications/{id}` to `api/app/salesforce.py`, same rate-limiting as create/update (reuse `_is_rate_limited`/`_client_ip`). **Before deleting, query the record's current `Status__c` server-side.** If it's `Archived`, reject with 403 and a clear message ("Archived records can't be deleted") and do not call Salesforce's delete. Otherwise call Salesforce's REST delete (`DELETE /services/data/{API_VERSION}/sobjects/Loan_Application__c/{id}`) for real. This check must happen in the endpoint itself, not just the UI — the UI hiding a button doesn't stop someone from calling the endpoint directly with a known ID.
  > 4. **UI**: a delete action per row, wired to the new endpoint, with loading/success/error state, removing the row from the list on success. Disable/hide the delete action client-side for rows with Status=Archived too (matches the server-side rule, avoids a pointless round trip to get a 403) — but the server-side check in #3 is the actual protection, not this.
  >
  > Verify: Archived is selectable in the filter but absent from the create dropdown and the per-row Status dropdown; attempting `DELETE` directly against one of the 3 seed record IDs (e.g. via curl, bypassing the UI) returns 403 and the record still exists in Salesforce afterward; deleting a non-Archived record (e.g. one created via the demo) actually removes it from Salesforce, confirmed by reloading the page and it staying gone.

  **Resolution:** `api/app/salesforce.py` — `LoanApplicationCreateRequest` gained a `status` field (default `"Draft"`), validated against a new `SETTABLE_STATUSES` tuple (Draft/Submitted/Under Review/Approved/Denied — Archived deliberately excluded) and threaded through to `create_loan_application`, which also now stamps `Submitted_Date__c` to `date.today().isoformat()` server-side (was never set before — the reported bug). Added `PATCH /salesforce/loan-applications/{id}` (`LoanApplicationUpdateRequest`, Status-only — Pydantic naturally drops any other field a client sends, verified) and `DELETE /salesforce/loan-applications/{id}`, both reusing `_is_rate_limited`/`_client_ip` (all three mutating endpoints share one rate-limit pool per IP). DELETE queries `Status__c` server-side first via a new `_get_status` helper — 404 if the record doesn't exist, 403 ("Archived records can't be deleted") without ever calling Salesforce's delete if it's Archived, otherwise a real `DELETE /sobjects/Loan_Application__c/{id}` call. Record IDs are validated against a `^[a-zA-Z0-9]{15,18}$` pattern (422 if malformed) before any Salesforce call, on both PATCH and DELETE. `web/src/components/portfolio/LoanDemoWidget.tsx` — added a Status `<select>` to the create form (`SETTABLE_STATUSES` options, defaults to Draft); sortable column headers (click toggles asc/desc with a ▲/▼ indicator, client-side `Array.sort` over the fetched list, nulls sort last); a Status filter `<select>` above the table (`All` + `SETTABLE_STATUSES` + `Archived` — 7 options) that composes correctly with sorting (filter narrows the rows, then sort orders what's left); a per-row Status `<select>` wired to PATCH with a `saving…`/error state, refreshing the full list on success (picks up any Decision Date the Flow set); a per-row delete button wired to DELETE with a `deleting…`/error state, removing the row from local state on success. For rows with `Status === "Archived"`: rendered the Status cell as plain text (not a select) and hid the delete button entirely — the task's explicit "hide for Archived" instruction only named the delete action, but the surrounding context note ("nothing in the UI can ever archive a record or un-archive one") implied the Status control needed the same treatment, so I hid both for internal consistency; flagging this judgment call in case Robin wants the per-row Status select visible-but-Archived-excluded-from-options instead. Verified backend logic exhaustively via FastAPI's `TestClient` with `_request` monkeypatched (21 assertions, no real Postgres/Salesforce needed since `TestClient` without the context-manager form skips app lifespan): create with Status=Approved sets `Status__c` and `Submitted_Date__c` correctly; create rejects Archived (422) and defaults to Draft when omitted; PATCH updates correctly, rejects Archived and garbage status values (422), rejects malformed record IDs (422), and — confirming the "scoped" requirement — sends only `{"Status__c": ...}` to Salesforce even when the client's JSON body includes other fields; DELETE on an Archived-status record returns 403 with zero Salesforce delete calls made, succeeds on a non-archived record, 404s on an unknown ID, 422s on a malformed ID; rate limiting is shared and correctly triggers 429 on the 6th mutating request within the window; existing honeypot/GET-list behavior unaffected. Verified the widget UI against a local mock server (list+create+patch+delete, including an Archived seed record): create-form and filter option counts exactly as specified; Archived row has no select/delete button; sorting Amount ascending then descending reorders correctly; filtering to Archived shows only that row; changing a row's Status to Approved via the UI shows the Decision Date populate immediately (matching the mock's simulated Flow) and the row's own select reflects the new value; deleting a non-archived row removes it from the table; the Archived row survives both other rows' mutations undisturbed. Caught a real off-by-one bug in my own mock server's path-parsing during this (not the actual app) and fixed it before trusting the result. `npm run build` clean, zero console warnings, no JSX whitespace-glue issues in the full-text dump. Not verifiable without Robin's real org/credentials: the literal "curl DELETE against a real LA-000x seed ID returns 403" check, and the live Decision Date Flow actually firing in production — both his to confirm.

- [x] 2026-07-07 — Confirmed live (Robin tried it against production): `POST /salesforce/loan-applications` requires the typed Applicant/Account names to already exist as Contact/Account records in Salesforce, or it 422s with "Unknown applicant or account." That's a bad live-demo experience — an interviewer trying the form with a made-up name hits a confusing dead end on the first attempt.

  **Handoff given to CLI (2026-07-07):**
  > Change `create_loan_application` in `api/app/salesforce.py` from "find or fail" to "find or create" for both the Applicant (Contact) and Account lookups. Currently `_find_id_by_name` raises `SalesforceLookupError` (surfaced as a 422) when no match exists. Instead: if no existing Contact/Account matches the given name, create a minimal new one (Contact: just `LastName` — split the typed name naively, e.g. last word as LastName, everything before it as FirstName if present; Account: just `Name`) and use its new Id, rather than erroring. Keep the existing exact-match path for names that already exist (so the seed data / repeated demo names still resolve to the same record rather than duplicating). Same honeypot/rate-limit/validation protections stay as-is — this only changes what happens after those checks pass and a lookup misses. Update the `SalesforceLookupError`/422 test coverage accordingly (there should no longer be a code path that raises it for a genuinely missing name — only for something structurally invalid, if that still applies). Verify: creating with a brand-new name succeeds and the new Contact/Account are visible in Salesforce afterward; creating with an existing seed name (e.g. `John Smith`/`Acme Rural Holdings`) still reuses the existing records rather than duplicating them. `npm run build` / relevant API tests clean. Check off here when done.

  **Resolution:** renamed `_find_id_by_name` to `_find_or_create_id` — on a SOQL miss it now calls a new `_create_sobject` helper instead of raising, using `_contact_create_fields()` (naive last-word-as-LastName split, FirstName omitted for single-word names) for Contact and `{"Name": name}` for Account. `create_loan_application` reuses `_create_sobject` for the `Loan_Application__c` record too, for consistency. `SalesforceLookupError` had no remaining legitimate use once the only code path that raised it was removed (name-splitting can't produce an empty required field given the route's existing non-empty validation, and any real Salesforce-side failure is already `SalesforceAPIError`), so it and its route-level `except` clause were deleted rather than left as dead code. No repo test files existed to update (all prior verification in this project has been manual/scripted, not committed pytest). Verified the actual business logic directly against a monkeypatched `_request` (since the token/API endpoints are hardcoded to `https://`, a real HTTP mock server wasn't a clean option): existing seed names (`John Smith`/`Acme Rural Holdings`) resolve to their existing Ids with zero Contact/Account creates; a brand-new name pair creates exactly one new Contact (with correct FirstName/LastName split) and one new Account and uses their real returned Ids on the Loan Application; a single-word name produces a LastName-only Contact; calling with the same brand-new name twice only creates the underlying records once (the second call's SOQL lookup finds what the first call created). Also re-ran the full honeypot/too-fast/rate-limit/CORS/clean-502-on-unreachable-Salesforce regression suite from the original `salesforce-loan-demo` change against the real route — all still pass unchanged; confirmed a legitimate request with a fake Salesforce domain now fails with 502 (Salesforce unreachable) rather than the old 422, i.e. the "unknown name" 422 path is genuinely gone. `npm run build` (`/web`, unaffected by this backend-only change) clean.

- [x] 2026-07-07 — Update the `/portfolio` page's "professional-reputation graph" info-bubble copy — Robin shared the real mechanics from Farpost's actual `reputation.py` service and the current copy undersells it (doesn't clarify it's explicitly *not* a score, and misses the quarantine/trust-gate detail).

  **Handoff given to CLI (2026-07-07):**
  > Replace the `InfoTooltip` copy next to "professional-reputation graph" in `web/src/app/portfolio/page.tsx` (the one you just built) with this text — same component, same placement, just swap the string:
  >
  > "Farpost's professional-reputation graph: not a score or star rating — a computed timeline. Every action a professional takes (inspections, claims, relationships with buildings) is an event; reputation is assembled fresh from those events on each request, surfacing activity history and breadth of engagement rather than a single number. Professionals whose roles haven't yet earned trust are excluded entirely — no score, no timeline, nothing to game. It's the same underlying entities-connected-by-records shape as a Salesforce Account/Contact view, just applied to a professional network instead of a household."
  >
  > No structural/behavioral changes needed to `InfoTooltip.tsx` itself — this is a copy-only swap. Confirm `npm run build` still clean. Check off here when done.

  **Resolution:** swapped the `PROFESSIONAL_REPUTATION_GRAPH_EXPLANATION` string constant in `web/src/app/portfolio/page.tsx` — no changes to `InfoTooltip.tsx` or the wiring around it. Verified via headless browser: rendered tooltip text matches the new copy byte-for-byte, and re-checked the core interaction behaviors (hover open/close, keyboard focus opens it, Escape dismisses) still work unchanged. `npm run build` clean, no new console warnings.

- [x] 2026-07-07 — Robin wants an info-bubble on the `/portfolio` page next to "professional-reputation graph" (in the `FARPOST_PARALLEL` section, `web/src/app/portfolio/page.tsx` line ~76) that fully explains the term for a visitor who doesn't have Farpost context.

  **Handoff given to CLI (2026-07-07):**
  > Add an accessible info-bubble/tooltip next to the phrase "professional-reputation graph" in the `FARPOST_PARALLEL` section of `web/src/app/portfolio/page.tsx`. No existing tooltip/popover component exists on this site — build a small one (e.g. an "ⓘ" icon button that reveals the explanation on hover/focus and on click, keyboard-accessible, dismissible), styled per the site's monospace/terminal aesthetic and single accent color. Use this exact explanatory copy (already drafted, don't reword the substance):
  >
  > "Farpost's professional-reputation graph: a network connecting insurance-industry professionals — adjusters, inspectors, contractors, agents — and building owners through the actual jobs they've worked together, with reputation and verification signals attached to each relationship. It's the same underlying data shape as a Salesforce Account/Contact household view: distinct entities connected by records that carry status and history — just applied to a professional network instead of a family."
  >
  > Verify it's keyboard-accessible (focusable, dismissible with Escape) and doesn't break `npm run build`. Check off here when done.

  **Resolution:** built `web/src/components/InfoTooltip.tsx` — a small ⓘ icon button styled with the site's accent color/monospace conventions, next to "professional-reputation graph" in `portfolio/page.tsx`. Caught and fixed two real interaction bugs during verification, not just cosmetic ones: (1) a naive single-state open/close toggle meant hovering-then-clicking (what actually happens on every mouse click, since Playwright/browsers move the pointer there first) immediately closed the tooltip the click was supposed to pin open — fixed by splitting into independent `hovered`/`focused`/`pinned` states ORed together, so a click toggles `pinned` without being clobbered by hover; (2) Chromium focuses a button on mouse click too, which meant un-pinning via a second click didn't actually close the tooltip since residual click-focus kept it open — fixed by only setting `focused` on `:focus-visible` (keyboard/programmatic focus), not plain click-focus. Verified via headless browser: hover-only open/close cycle, click-pins-open-beyond-hover, second-click-unpins-when-not-hovering, Tab-to-focus opens it, Escape closes it while retaining focus, Enter (keyboard-only activation) pins it, and click-outside closes it — all confirmed. Tooltip text content compared byte-for-byte against the required copy — exact match. `npm run build` clean, no console warnings/errors beyond a pre-existing, unrelated `LoanDemoWidget` fetch failure (no local API running in this dev session).

- [x] 2026-07-07 — New OpenSpec change `salesforce-loan-demo` proposed and validated (not a bug — logged here per the handoff-logging convention since it's a "paste this to CLI" moment). Adds the Salesforce integration portfolio case study: a custom `Loan_Application__c` object model in Robin's Salesforce Developer Edition org, an httpx-based OAuth2 Client Credentials Flow client with token caching, `GET`/`POST /salesforce/loan-applications` API endpoints, and a rewritten `/portfolio` page with case-study content and a live demo widget. Full detail in `openspec/changes/salesforce-loan-demo/`.

  **Handoff given to CLI (2026-07-07):**
  > Implement the validated OpenSpec change at `openspec/changes/salesforce-loan-demo/` — read `proposal.md` and `design.md` for context, then work through `tasks.md` (6 groups). **Skip section 1 entirely** — that's Salesforce object-model setup Robin does manually in Setup UI, not a CLI task; it may or may not be done yet when you start. Sections 2-6 are yours: the OAuth2 Client Credentials Flow client (`api/app/salesforce.py`, raw `httpx`, not `simple-salesforce` — deliberate choice to demonstrate protocol mechanics), an in-memory expiry-aware token cache (reuse a valid token, don't refetch per request), the `GET`/`POST /salesforce/loan-applications` endpoints (the POST reuses `contact-form`'s honeypot + minimum-fill-time + per-IP rate-limiting pattern verbatim — same abuse-resistance bar, since this writes real records to a live org with a finite daily API quota), CORS needs `GET` added alongside the existing `POST`-only allowance, and new env vars `SALESFORCE_DOMAIN`/`SALESFORCE_CLIENT_ID`/`SALESFORCE_CLIENT_SECRET` following the existing `RESEND_API_KEY` pattern (local `.env` + Railway config vars). On the web side, replace the `/portfolio` placeholder with real case-study content — it must explicitly state that Financial Services Cloud and Agentforce weren't used because they need paid licenses this free Developer Edition org doesn't have (don't imply parity with a production Salesforce implementation), plus the structural parallel to Farpost's professional-reputation graph — and a live demo widget (list + create, loading/success/error states), styled per the existing monospace/`SectionHeader`/single-accent-color convention. Verification section includes updating `docs/stack.md` and logging this session's `invalid_client_id` External Client App debugging saga to `docs/sreditor/` (see `design.md`'s Risks section for the details — genuine unresolved platform quirk, resolved by recreating the app from scratch, worth capturing as SR&ED-relevant experimentation). Check off tasks as you go and report back when done so this can go through the drift-audit/archive step.

  **Resolution:** implemented sections 2-6 (section 1 correctly skipped — Robin's manual Salesforce Setup work). `api/app/salesforce.py` implements the OAuth2 Client Credentials Flow via raw `httpx` with an in-memory expiry-aware token cache (Salesforce's client-credentials response has no `expires_in`, so it assumes a conservative TTL for proactive refresh, backed by a reactive 401-retry for whenever that assumption is wrong in either direction) plus SOQL query and sobject-create helpers. `GET`/`POST /salesforce/loan-applications` added, with the `POST` reusing `contact-form`'s honeypot/min-fill-time/rate-limiting pattern verbatim (own independent limiter state, not shared with `/contact`'s). CORS gained `GET`. Caught and fixed a real bug during verification: connection-level `httpx` errors (DNS failure, timeout) weren't being caught, only HTTP-status errors were, so a Salesforce-unreachable scenario produced a raw 500 with a traceback instead of the intended clean 502 — fixed by wrapping both the token-fetch and API-request calls in `try/except httpx.RequestError`. Verified against the real route code with fake-but-realistic env vars: honeypot/too-fast submissions return 201 without ever attempting a Salesforce call; a burst of requests triggers 429 past the 5-request/60s threshold; legitimate requests fail cleanly with a generic 502 and no leaked secrets (confirmed via server-side log vs. client response). Verified CORS preflight for both an allowed and disallowed origin. On the web side, rewrote `/portfolio` with the case-study copy (architecture, the Client Credentials Flow/raw-httpx rationale, the explicit FSC/Agentforce licensing-limitation statement, the Farpost parallel) plus a `LoanDemoWidget` component with list/create/loading/success/error states, verified against a local mock server shaped like the real endpoint response (list renders, create flow works end-to-end, row count updates on success) since CLI has no real Salesforce credentials or org access. Caught and fixed the same JSX whitespace-glue bug from earlier sessions in three more spots (`Loan_Application__cobject`, `simple-salesforcewrapper`) via explicit `{" "}`. `npm run build` clean, zero console warnings throughout. Updated `docs/stack.md` and logged the `invalid_client_id`/External Client App saga to `docs/sreditor/2026/2026-07-07-salesforce-external-client-app-invalid-client-id.md`. Tasks 5.1/5.2 and all of section 6 (Railway env vars, deploy, production verification) are Robin's to complete — CLI has no Salesforce org, Railway, or Vercel access.

- [x] 2026-07-07 — New OpenSpec change `home-navigation-links` proposed and validated. Adds a "Home" menu entry (first position) and makes the "$ Robin Samways" header text a link to `/` — currently there's no way back to the homepage from a placeholder page other than the browser back button. Full detail in `openspec/changes/home-navigation-links/`.

  **Handoff given to CLI (2026-07-07):**
  > Implement the validated OpenSpec change at `openspec/changes/home-navigation-links/` — read `proposal.md` for context (small change, no design.md needed), then work through `tasks.md`. Two things: add "Home" as the first item in the menu (above Portfolio/Farpost/Dev Log), and make the "$ Robin Samways" header text a link to `/`, available on the homepage and all three placeholder pages. Watch for the fixed-width toggle button from the earlier layout-shift fix — don't reintroduce that issue while touching the header. Check off tasks in `tasks.md` as you go and report back when done.

  **Resolution:** added "Home" as the first entry in `MenuToggle.tsx`'s links array (above Portfolio/Farpost/Dev Log), and wrapped the "$ Robin Samways" text in `Header.tsx` in a `next/link` to `/`, keeping the existing `$` accent span untouched. Verified via headless browser across all four routes (`/`, `/portfolio`, `/farpost`, `/dev-log`): menu order is Home/Portfolio/Farpost/Dev Log; both the header title link and the Home menu link navigate to `/` correctly; and — specifically checking for a regression of the earlier layout-shift fix — toggled the menu button 6 times on every route, confirming the button stays a constant 32×32px and the header title's x-position never moves (184px throughout). `npm run build` clean, zero console warnings/errors across all checks.

- [x] 2026-07-07 — Production contact form failed with a browser console CORS error: `Access to fetch at 'http://localhost:8000/contact' from origin 'https://robinsamways.ca' has been blocked by CORS policy`. Root cause: `ContactForm.tsx` reads `NEXT_PUBLIC_API_URL`, falling back to `localhost:8000` when unset — Vercel never had that variable configured for Production, so the deployed build silently pointed at a dev-only address. This was actually anticipated in the `/ops/deploy` runbook page's Vercel setup steps ("no environment variables are required yet... that's when `NEXT_PUBLIC_API_URL` gets added"), just not yet acted on.

  **No CLI handoff needed** — this was a Vercel dashboard config fix, not a code change. Robin added `NEXT_PUBLIC_API_URL=https://api.robinsamways.ca` as a Production environment variable (Project → Settings → Environment Variables) and triggered a redeploy. Verified: console error gone, submission persisted, Resend notification email arrived. Also updated `docs/deployment-guide.md` Part 3 to mark this variable as now-required rather than "for later."

- [x] 2026-07-07 — New OpenSpec change `contact-form` proposed and validated (not a bug — logged here per the handoff-logging convention since it's a "paste this to CLI" moment). Adds a homepage Contact section, a `POST /contact` API endpoint, a `ContactSubmission` Postgres table, and a Resend email notification. Full detail in `openspec/changes/contact-form/`.

  **Handoff given to CLI (2026-07-07):**
  > Implement the validated OpenSpec change at `openspec/changes/contact-form/` — read `proposal.md` and `design.md` for context, then work through `tasks.md` top to bottom (5 groups: API data model, API endpoint, web form UI, verification, deploy). Key design decisions already made (see `design.md` for rationale): DB write happens before the Resend email send and the request still succeeds even if the email fails; CORS is restricted to `https://robinsamways.ca`, `https://www.robinsamways.ca`, and `http://localhost:3000` — no wildcard; spam mitigation is a honeypot field + minimum-fill-time check (silently drop, don't error) plus a simple in-memory per-IP rate limiter — no CAPTCHA, no Redis; Resend is called via a direct `httpx` REST call, not its SDK. The existing `RESEND_API_KEY` Railway env var should already cover this — confirm rather than adding a new one. Check off tasks in `tasks.md` as you go, update `docs/stack.md` per task 4.6, and report back when done so this can go through the drift-audit/archive step.

  **Resolution:** implemented end-to-end — `ContactSubmission` SQLModel, `POST /contact` with CORS/honeypot/rate-limiting/Resend-via-httpx exactly per the design decisions, and the `Contact` form section on the homepage. All of tasks.md groups 1-4 verified locally (valid submission persists + returns 201 even without a real Resend key configured; honeypot and too-fast submissions silently dropped with no DB row; rate limiter 429s after 5 requests/60s; CORS preflight confirmed allowed vs. disallowed origins; `npm run build` clean). Also fixed a latent bug surfaced by the first real INSERT against `created_at`: the column was `TIMESTAMP WITHOUT TIME ZONE` while the code wrote a tz-aware datetime, causing a DBAPI error — fixed via an explicit `DateTime(timezone=True)` column on both `Ping` and `ContactSubmission`. `RESEND_API_KEY` confirmed reused, not re-added. Pushed to `origin/main` per Robin's go-ahead; production redeploy confirmation and the live end-to-end test (tasks.md 5.2/5.3) are his to complete since CLI has no Railway/Vercel dashboard access.

- [x] 2026-07-07 — Header shifts a couple of pixels when the menu toggle switches between ☰ and ✕ — the two glyphs have different natural widths in the monospace font, so the button (and everything to its right, including the "$ Robin Samways" title) reflows on toggle.

  **Handoff given to CLI (2026-07-07):**
  > Fix the layout shift in `web/src/components/MenuToggle.tsx`: the toggle button currently has no fixed width, so switching its content between "☰" and "✕" changes the button's width slightly (different glyph widths) and shifts the header text next to it by a couple of pixels. Give the button a fixed width/height (e.g. a fixed-size flex container with the glyph centered inside) so toggling never changes its footprint. Verify by toggling the menu open/closed repeatedly and confirming the header title doesn't move. Check off in `docs/issues.md` when done.

  **Resolution:** gave the toggle `<button>` a fixed `h-8 w-8` flex container (`flex items-center justify-center`) in `web/src/components/MenuToggle.tsx`, so its footprint no longer depends on the glyph's natural width. Verified by toggling the button 6 times via headless browser and measuring both the button's bounding box and the header title's x-position on each toggle — button stayed at a constant 32×32px, title x-position never moved (184px every time).

- [x] 2026-07-07 — Headshot image (`/images/headshot.png` via `next/image` in `Header.tsx`) throws a repeated console warning: "Image with src ... has either width or height modified, but not the other. ... include the styles 'width: \"auto\"' or 'height: \"auto\"'." Likely a Tailwind class constraining one dimension without the matching `auto`. Seen on every route (`/`, `/portfolio`, `/farpost`, `/thoughts`).

  **Handoff given to CLI (2026-07-07):**
  > Fix the console warning flagged in `docs/issues.md`: the headshot image (`web/src/components/Header.tsx`, `next/image` with `src="/images/headshot.png"`) throws a repeated Next.js warning on every route — "has either width or height modified, but not the other... include the styles `width: "auto"` or `height: "auto"`." Track down the Tailwind class constraining one dimension without the matching `auto` and fix it so the warning clears.
  >
  > Note: a root `CLAUDE.md` now exists in the repo with the Chat/CLI role split, the OpenSpec workflow, `docs/issues.md` (lightweight QA flags), and `docs/sreditor/` (contemporaneous SR&ED R&D notes — only log something there if it involves genuine technological uncertainty, not routine fixes like this one). Check off the item in `docs/issues.md` once resolved, verify no console warning on any of the four routes, and report back.

  **Resolution:** Root cause was Tailwind's preflight base layer, which sets `img { max-width: 100%; height: auto; }` globally. That `height: auto` fought the explicit `width={96} height={96}` props on the `next/image` element (nothing in `Header.tsx` set a matching CSS height), which is exactly the "one dimension modified, not the other" case Next.js warns about. Fixed by adding `h-24 w-24` (96px, matching the `width`/`height` props) to the image's className in `web/src/components/Header.tsx`, so both dimensions are explicitly and consistently set. Verified with a headless-browser console check across `/`, `/portfolio`, `/farpost`, `/thoughts` — zero warnings/errors on any route.

- [x] 2026-07-07 — Robin flagged (by eye, via screenshot) that the headshot still looked visually wrong after the console-warning fix above: a dark navy rounded-square shadow visible behind/below-left of the photo. Initially misdiagnosed as a stale Hot Module Reload paint artifact (a fresh headless-browser screenshot appeared clean at a glance); Robin's hard-refresh screenshot proved it was still there, and a closer high-DPI crop confirmed it.

  **Root cause:** the shadow was baked directly into the pixels of `web/public/images/headshot.png` itself — not a CSS bug at all. That file had been extracted from the embedded image inside `resume.pdf` in an earlier session; the PDF's design applied a drop-shadow + rounded-corner frame to the photo, and that got rasterized into the same bitmap before being embedded, so extracting "the headshot" from the PDF pulled out the shadow along with it.

  **Resolution:** cropped `headshot.png` down to just the photo content (300×300 region inset from the shadow/frame, trimmed to 293×294 with a small safety margin), removing the baked-in shadow entirely and letting the existing CSS (`rounded-xl` + `object-cover`) apply the rounded-corner treatment instead. Also had to clear Next's dev image-optimizer cache (`web/.next/dev/cache/images/`) — it was serving the old rasterized shadow from cache even after the source file changed, which is why the first re-check after the crop still showed the shadow. Verified with a fresh high-DPI screenshot of the rendered element (clean, no shadow) and a full console check across all four routes (zero warnings/errors).

- [x] 2026-07-10 — Farpost Pulse's landing/index copy named a real company and a named individual as the origin story for the page, framed as a direct response to their specific interview feedback. Robin decided this made an unverifiable claim about a real company's product/feedback and asked for it to be replaced with a more generic, defensible framing — a curiosity-driven gap between knowing a stack on paper and having genuine hands-on time in it — not a wording tweak, six exact spots given verbatim rather than loose direction, since the page was live with the old copy at the time.

  **Handoff given to CLI (2026-07-10):**
  > Spec's fixed. Six exact verbatim replacements, given in full rather than vague direction since this is live right now: `web/src/app/narrative/farpost-pulse/page.tsx`'s subtitle, both Origin Story paragraphs, the Architecture section's trailing sentence about Python vs. Node.js, and the Tech Stack table's Backend row "Why" cell; plus `web/src/app/narrative/page.tsx`'s Farpost Pulse index teaser. All six drop the named-company/named-individual/"specific interview feedback" framing in favor of "wanted genuine hands-on time with a stack I wanted to actually know, not just read about."

  **Resolution:** applied all six replacements verbatim (converting straight apostrophes to `&rsquo;` for the JSX-text spots, per the site's existing typographic convention; left the `narrative/page.tsx` teaser as a plain TS string literal, unchanged convention-wise, since it's interpolated via `{}` rather than literal JSX text). No other text on either page — section IDs/labels, the rest of the Tech Stack table, Design Notes, Tech Roster intro — was touched. Verified via a full rendered-text extraction of both pages' server-rendered HTML (not just source review) — all six spots read exactly as specified, no JSX whitespace-glue regressions despite several new em-dashes landing near existing `<code>`/`{" "}` boundaries. `npm run build` clean, zero console warnings on either route.
